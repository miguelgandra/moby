% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{rays}
\alias{rays}
\alias{rays_detections}
\alias{rays_tags}
\alias{rays_deployments}
\title{Toy acoustic-telemetry dataset (two rays in an MPA)}
\format{
\code{rays_detections}: a data frame with one row per detection and columns \code{ID}, \code{datetime}
(POSIXct, UTC), \code{station}, \code{lon}, \code{lat}, \code{species}, \code{receiver}, \code{transmitter}.

\code{rays_tags}: a data frame with columns \code{ID}, \code{transmitter}, \code{species}, \code{tagging_date} (POSIXct),
\code{tagging_station}.

\code{rays_deployments}: a data frame with columns \code{receiver}, \code{station}, \code{lon}, \code{lat}, \code{deploy},
\code{recover} (POSIXct) and \code{depth} - the canonical deployment-log schema (as produced by
\code{\link{importDeployments}}) used by \code{\link{checkDeployments}},
\code{\link{matchDeployments}} and \code{\link{plotDeployments}}.

\code{rays}: a \code{\link{mobyData}} (data-frame subclass) with the columns of \code{rays_detections} plus
a \code{timebin} column, and a \code{"moby"} metadata attribute (column mapping, \code{epsg.code} (32629, UTM 29N),
tagging dates and the per-species \code{id.groups}).

An object of class \code{data.frame} with 1643 rows and 8 columns.

An object of class \code{data.frame} with 8 rows and 5 columns.

An object of class \code{data.frame} with 6 rows and 7 columns.
}
\source{
Simulated with \code{data-raw/make-toy-data.R} (seeded, reproducible). Not derived from any
real tracking study.
}
\usage{
rays

rays_detections

rays_tags

rays_deployments
}
\description{
A small, \strong{fully synthetic} acoustic-telemetry dataset used throughout the moby
tutorials. It mimics two demersal elasmobranchs - thornback ray (\emph{Raja clavata}) and common
stingray (\emph{Dasyatis pastinaca}) - monitored by a six-receiver array inside a small coastal
marine protected area (MPA). Coordinates, dates and detection patterns are simulated (no real
protected-species locations are involved), but are designed to be ecologically plausible enough
to exercise the whole moby workflow, including per-species analyses via \code{id.groups}.

The objects come as a small family so that each tutorial can start from the appropriate
checkpoint without re-running earlier ones:
\itemize{
\item \code{rays_detections} - raw, harmonised detections (a plain data frame).
\item \code{rays_tags} - tag/animal metadata (one row per animal).
\item \code{rays_deployments} - receiver-deployment log (one row per station).
\item \code{rays} - the cleaned, analysis-ready \code{\link{mobyData}} checkpoint (detections with a
\code{timebin} column and metadata attached, including the two-species \code{id.groups}).
}
Raw CSV versions for the import tutorial are also shipped: the detections in a generic
hand-mapped layout at \code{system.file("extdata", "rays_detections.csv", package = "moby")}, and
the receiver-deployment log in raw ETN (European Tracking Network) export format at
\code{system.file("extdata", "rays_deployments.csv", package = "moby")} (harmonise it with
\code{importDeployments(..., source = "etn")}).
}
\examples{
# the analysis-ready mobyData checkpoint (metadata attached)
data(rays)
is_moby(rays)
mobyMeta(rays)$id.groups        # the two-species grouping
head(rays)

# the accompanying raw tables
head(rays_detections)           # harmonised detections (plain data frame)
head(rays_tags)                 # one row per tagged animal
head(rays_deployments)          # receiver-deployment log
}
\keyword{datasets}
