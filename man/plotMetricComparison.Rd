% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotMetricComparison.R
\name{plotMetricComparison}
\alias{plotMetricComparison}
\title{Compare metrics across the levels of a grouping factor}
\usage{
plotMetricComparison(
  data,
  metrics,
  id.col = NULL,
  split.by,
  metric.labels = NULL,
  agg.fun = function(x) mean(x, na.rm = TRUE),
  plot.type = c("box", "violin", "points"),
  paired = TRUE,
  test = c("auto", "none"),
  p.adjust.method = "holm",
  min.n = 6,
  flag.significant = FALSE,
  alpha = 0.05,
  discard.incomplete = TRUE,
  display.n = FALSE,
  color.pal = NULL,
  background.color = "grey96",
  outliers = FALSE,
  main = NULL,
  ncol = 2,
  cex = 1,
  file = NULL,
  width = NULL,
  height = NULL,
  res = 300
)
}
\arguments{
\item{data}{A \code{mobyData} or data frame containing the metric column(s), an individual column and
the grouping column.}

\item{metrics}{Character vector of numeric column names to compare (one panel each).}

\item{id.col}{Name of the column containing animal IDs. Defaults to \code{"ID"}.}

\item{split.by}{Name of the grouping column (the within-individual factor: diel phase, season, ...).}

\item{metric.labels}{Optional display names for the panels (defaults to the column names).}

\item{agg.fun}{Function aggregating multiple rows per individual x level to one value. Defaults to
the mean.}

\item{plot.type}{One of "box" (default), "violin" or "points".}

\item{paired}{Logical; treat the design as repeated-measures (within-individual). Defaults to TRUE.
Set FALSE only for genuine independent-groups factors (e.g. sex, population).}

\item{test}{One of "auto" (default; run the design-appropriate test), or "none".}

\item{p.adjust.method}{Multiple-comparison correction across the metric panels, passed to
\code{\link[stats]{p.adjust}}. Defaults to "holm".}

\item{min.n}{Minimum complete pairs/blocks required to run a test. Defaults to 6.}

\item{flag.significant}{Logical; if TRUE, outline panels whose adjusted p is below \code{alpha}.
Defaults to FALSE (avoids dichotomous "significant/not" reading).}

\item{alpha}{Significance threshold for \code{flag.significant}. Defaults to 0.05.}

\item{discard.incomplete}{Logical; if TRUE (default), drop individuals missing from any level for
the \emph{plotted distributions} too (so the boxes match the tested set). If FALSE, all available
values are plotted while the test still uses complete blocks.}

\item{display.n}{Logical; append the per-level sample size to the x-axis labels. Defaults to FALSE.}

\item{color.pal}{Fill colours, one per level. If NULL, a colourblind-safe palette is used.}

\item{background.color}{Panel background colour. Defaults to "grey96".}

\item{outliers}{Logical; show boxplot outliers. Defaults to FALSE.}

\item{main}{Optional overall title.}

\item{ncol}{Number of columns in the panel layout. Defaults to 2.}

\item{cex}{Global expansion factor for all plot text. Defaults to 1.}

\item{file}{Optional output file. If \code{NULL} (the default), the figure is drawn on the current
graphics device - the usual interactive behaviour. If a file path is supplied, moby opens a
graphics device chosen from the file extension (\code{.pdf}, \code{.svg}, \code{.png}, \code{.jpg}/\code{.jpeg},
\code{.tif}/\code{.tiff} or \code{.bmp}), draws the figure to it, and closes the device automatically (also if
an error occurs). For multi-page or batch workflows, keep \code{file = NULL} and manage the device
yourself (e.g. \verb{pdf(...); plot...(); dev.off()}).}

\item{width, height}{Output size in inches. Used \emph{only} when \code{file} is supplied. If \code{NULL}
(default), a size is derived from the figure's structure (e.g. the number of individuals or
panels). These defaults are sensible starting points, \emph{not} guarantees: dense or unusual figures
may still need you to set \code{width}/\code{height} explicitly. The two can be set independently.}

\item{res}{Resolution in pixels per inch, for raster formats only (\code{.png}, \code{.jpg}, \code{.tif},
\code{.bmp}); ignored for vector formats (\code{.pdf}, \code{.svg}). Used only when \code{file} is supplied. Defaults
to 300.}
}
\value{
Invisibly, a tidy per-metric data frame of the test results (test, paired, n's, statistic,
df, raw and adjusted p, effect size, method), with the per-(individual, level, metric) values
attached as \code{attr(., "values")}.
}
\description{
Draws a multi-panel comparison of one or more per-individual behavioural metrics (e.g.
activity rate, distance moved, home-range area, association index) across the levels of a single
grouping factor - typically a within-individual time-frame such as diel phase or season. One panel
per metric shows the distribution of the per-individual values at each level (box, violin or
points), and, optionally, annotates a design-appropriate group comparison test.
}
\details{
\strong{Statistical testing.} Comparisons of a per-individual metric across time-frames are
\emph{repeated-measures by design} (the same animals are measured under each level), so the default
tests respect within-individual pairing: \strong{Wilcoxon signed-rank} for 2 levels and the
\strong{Friedman test} for >2 levels (both on the individuals present in \emph{all} compared levels;
Skillings-Mack is used for incomplete blocks if that package is installed). Independent-groups
tests (Mann-Whitney / Kruskal-Wallis) are used only with \code{paired = FALSE}. An \strong{effect size}
(rank-biserial for signed-rank, Kendall's W for Friedman) is always computed, and p-values are
corrected across the metric panels (\code{p.adjust.method}). Tests are suppressed below \code{min.n}
complete pairs/blocks.

This annotation is a \strong{descriptive/exploratory aid, not confirmatory inference}: it is computed on
the complete-case subset, and telemetry non-detection is often \emph{informative} (an animal absent at
night because it left the array), so the comparison can be biased in ways no in-plot test corrects.
For confirmatory inference, fit a mixed-effects model (individual random effect; a distribution
suited to the metric) and check its diagnostics. Significance flagging is therefore \strong{off by
default}; the full test results are returned invisibly.

The metrics must already be columns in \code{data}; compute them upstream (e.g. with the \verb{calculate*}
family) so this function stays a pure visualiser. Multiple rows per individual x level are
aggregated by \code{agg.fun} (mean by default).
}
\note{
The layout is sized in inch units, so the figure stays consistent across devices.
}
\examples{
# Compare per-individual residency indices between the two species
res <- calculateResidency(rays, last.monitoring.date = max(rays$datetime))
res$species <- rays_tags$species[match(res$ID, rays_tags$ID)]
plotMetricComparison(res, metrics = c("IR1", "IR2"), split.by = "species",
                     paired = FALSE)
}
\seealso{
\code{\link{plotStationStats}}, \code{\link{plotGroupSizeDistribution}}
}
