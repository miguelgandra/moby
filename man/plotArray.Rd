% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotArray.R
\name{plotArray}
\alias{plotArray}
\title{Map the receiver array}
\usage{
plotArray(
  deployments,
  deployment.station.col = "station",
  deployment.lon.col = "lon",
  deployment.lat.col = "lat",
  deployment.deploy.col = "deploy",
  deployment.recover.col = "recover",
  detections = NULL,
  color.by = NULL,
  status.at = NULL,
  detection.range = NULL,
  range.color = NULL,
  range.alpha = 0.15,
  range.border = NA,
  hull = FALSE,
  hull.color = "grey40",
  hull.lty = 2,
  hull.lwd = 1,
  label = FALSE,
  label.cex = 0.7,
  label.color = "grey15",
  label.wrap = FALSE,
  pch = 21,
  pt.cex = 1.4,
  pt.color = "black",
  pt.bg = "white",
  pt.lwd = 1,
  land.shape = NULL,
  coastline = getOption("moby.coastline", TRUE),
  land.color = "gray50",
  epsg.code = NULL,
  background.layer = NULL,
  background.color = "#F3F7F7",
  background.pal = NULL,
  scale.km = NULL,
  scale.pos = "bottomright",
  scale.inset = 0.05,
  scale.height = 1.5,
  scale.color = "black",
  extent.factor = 1.15,
  legend = TRUE,
  main = NULL,
  cex = 1,
  file = NULL,
  width = NULL,
  height = NULL,
  res = 300,
  verbose = getOption("moby.verbose", TRUE)
)
}
\arguments{
\item{deployments}{A receiver-deployment log (a data frame), as produced by
\code{\link{importDeployments}} - one row per deployment, with at least the station and coordinate
columns. Multiple rows per station (repeated servicing) are reduced to one point per station.}

\item{deployment.station.col, deployment.lon.col, deployment.lat.col}{Names of the station,
longitude and latitude columns in the receiver-deployment log (\code{deployments}). Default to the
canonical \code{"station"}/\code{"lon"}/\code{"lat"} produced by \code{\link{importDeployments}}; set them when a
hand-made log uses other names (e.g. \code{deployment.lon.col = "Longitude"}). The \code{deployment.} prefix
marks these as deployment-log columns, keeping them distinct from the bare \verb{*.col} arguments,
which always refer to the detection dataset. The \code{receiver} column is the canonical join key and
is always taken as-is.}

\item{deployment.deploy.col, deployment.recover.col}{Names of the deployment and recovery
date-time columns in the receiver-deployment log (\code{deployments}). Default to the canonical
\code{"deploy"}/\code{"recover"} produced by \code{\link{importDeployments}}; set them when a hand-made log
uses other names (e.g. \code{deployment.deploy.col = "deploy_date"}). The \code{deployment.} prefix marks
these as deployment-log columns, keeping them distinct from the bare \verb{*.col} arguments, which
always refer to the detection dataset.}

\item{detections}{Optional. A detection dataset (data frame or \code{mobyData}). When supplied, stations
with zero detections are highlighted and counted - a quick check for dead receivers or coverage
gaps. The detection station column is taken from the dataset's \code{mobyData} metadata when present,
otherwise from the canonical \code{"station"}.}

\item{color.by}{Optional. Name of a \code{deployments} column used to colour the stations (e.g. habitat,
receiver model); a colourblind-safe Okabe-Ito palette and a legend are added. Takes precedence over
\code{status.at}.}

\item{status.at}{Optional. A single date; each station is classified and coloured as \code{"active"},
\code{"recovered"} or \code{"not yet deployed"} at that instant, from the deployment/recovery dates (a station is
active while \verb{deploy <= status.at < recover}). Character/\code{Date} input is interpreted in the data's
timezone (not the session's) for reproducibility.}

\item{detection.range}{Optional. Nominal detection radius in metres, drawn as a semi-transparent
circle around each receiver: a single value (applied to all stations), a numeric vector \strong{named by
station}, or the name of a \code{deployments} column holding a per-receiver radius. Stations with a
missing (\code{NA}) or zero radius get no circle. This is a visual aid for coverage assessment, NOT a
modelled detection probability.}

\item{range.color}{Fill colour of the detection-range circles. If NULL, a muted blue is used.}

\item{range.alpha}{Opacity of the range circles, from 0 (transparent) to 1 (opaque). Defaults to 0.15.}

\item{range.border}{Border colour of the range circles. Defaults to NA (no border).}

\item{hull}{Logical. If TRUE, outline the convex hull of the stations (the array footprint).
Defaults to FALSE.}

\item{hull.color, hull.lty, hull.lwd}{Colour, line type and width of the hull outline.}

\item{label}{Draw station labels: \code{FALSE} (default) none, \code{TRUE} the station name, or the name of a
\code{deployments} column to label by.}

\item{label.cex, label.color}{Size and colour of the station labels.}

\item{label.wrap}{Logical. If TRUE, wrap multi-word labels onto separate lines. Defaults to FALSE.}

\item{pch}{Plotting symbol for the stations (a fillable symbol such as 21-25 uses \code{pt.bg}).
Defaults to 21.}

\item{pt.cex, pt.color, pt.bg, pt.lwd}{Size, border colour, fill colour and border width of the station
symbols. \code{pt.bg} is overridden when \code{color.by}/\code{status.at} is used.}

\item{land.shape}{Optional. An \code{sf} object with coastlines/landmasses, drawn underneath the array.}

\item{coastline}{When the map is projected (an \code{epsg.code}, \code{land.shape} or \code{background.layer} is
given) and no \code{land.shape} is supplied, draw a default coastline for the extent. \code{TRUE} auto-picks a
scale; a string forces an \code{rnaturalearth} scale; \code{FALSE} draws none. Defaults to
\code{getOption("moby.coastline", TRUE)}. Requires the (Suggested) \code{rnaturalearth} (with
\code{rnaturalearthdata}) or \code{maps} package.}

\item{land.color}{Colour of land areas. Defaults to "gray50".}

\item{epsg.code}{Optional. A projected coordinate reference system (numeric EPSG code or \code{crs}
object, in metre units) used to project the coordinates and map layers. Required for the scale bar;
when omitted (and no map layer is supplied) the array is drawn in geographic (longitude/latitude)
space with coordinate axes.}

\item{background.layer}{Optional. A projected raster displayed in the background (e.g. bathymetry).}

\item{background.color}{Solid colour for the plot background when no \code{background.layer} is supplied.
Defaults to "#F3F7F7".}

\item{background.pal}{Colour palette for the \code{background.layer} raster. If NULL, a muted
colourblind-safe viridis palette is used.}

\item{scale.km}{Length of the scale bar, in kilometres. If NULL, chosen automatically. Projected
maps only.}

\item{scale.pos}{Position of the scale bar (a keyword such as "bottomright"). Defaults to "bottomright".}

\item{scale.inset}{Inset of the scale bar from the plot edges. A single value or a length-2 (x, y)
vector. Defaults to 0.05.}

\item{scale.height}{Thickness of the scale bar. Defaults to 1.5.}

\item{scale.color}{Colour of the scale-bar labels. Defaults to "black".}

\item{extent.factor}{Numeric factor to expand the plotting region around the stations. A value of 1
keeps the bounding box; values > 1 add a margin. Defaults to 1.15.}

\item{legend}{Logical. Draw a legend when \code{color.by}/\code{status.at}/\code{detections} is used. Defaults to TRUE.}

\item{main}{Optional title. If NULL, "Receiver array" is used; FALSE omits it.}

\item{cex}{Global expansion factor scaling every text element. Defaults to 1.}

\item{file}{Optional output file. If \code{NULL} (the default), the figure is drawn on the current
graphics device - the usual interactive behaviour. If a file path is supplied, moby opens a
graphics device chosen from the file extension (\code{.pdf}, \code{.svg}, \code{.png}, \code{.jpg}/\code{.jpeg},
\code{.tif}/\code{.tiff} or \code{.bmp}), draws the figure to it, and closes the device automatically (also if
an error occurs). For multi-page or batch workflows, keep \code{file = NULL} and manage the device
yourself (e.g. \verb{pdf(...); plot...(); dev.off()}).}

\item{width, height}{Output size in inches. Used \emph{only} when \code{file} is supplied. If \code{NULL}
(default), a size is derived from the figure's structure (e.g. the number of individuals or
panels). These defaults are sensible starting points, \emph{not} guarantees: dense or unusual figures
may still need you to set \code{width}/\code{height} explicitly. The two can be set independently.}

\item{res}{Resolution in pixels per inch, for raster formats only (\code{.png}, \code{.jpg}, \code{.tif},
\code{.bmp}); ignored for vector formats (\code{.pdf}, \code{.svg}). Used only when \code{file} is supplied. Defaults
to 300.}

\item{verbose}{Logical; print the array summary to the console. Defaults to
\code{getOption("moby.verbose", TRUE)}.}
}
\value{
Invisibly, a data frame of the plotted stations (station, receiver count, coordinates, and -
when requested - the \code{color.by} value, deployment status and detection flag). Called mainly for its
side effect (the array map).
}
\description{
Draws the spatial configuration of a receiver array from a deployment log: one point
per station, optionally over a coastline and/or background raster (e.g. bathymetry), with a metric
scale bar. It is a pre-analysis quality-control tool - use it to inspect the array layout, spot
implausible receiver coordinates, and judge spacing and overall coverage before running any
downstream analysis.

Semi-transparent circles of a user-defined radius (\code{detection.range}) can be drawn around each
receiver to give a quick visual sense of nominal coverage and potential detection gaps, and the
array's convex hull can be outlined as a footprint. Stations can be coloured by any metadata column
(\code{color.by}) or by their deployment status at a chosen date (\code{status.at}), and - if a detection
dataset is supplied - stations that never logged a detection are highlighted.

This is the spatial companion to \code{\link{checkDeployments}} (numeric QC) and
\code{\link{plotDeployments}} (temporal coverage), and follows the same mapping conventions as
\code{\link{plotMovements}} and \code{\link{plotMaps}}.
}
\examples{
# geographic quick look (no projection needed), with 800 m nominal detection ranges
plotArray(rays_deployments, detection.range = 800, label = TRUE)

\donttest{
# projected map with an automatic coastline and the array footprint
plotArray(rays_deployments, epsg.code = 32629, detection.range = 800,
          hull = TRUE, coastline = FALSE)
}
}
\seealso{
\code{\link{checkDeployments}}, \code{\link{plotDeployments}}, \code{\link{importDeployments}},
\code{\link{plotMovements}}, \code{\link{plotMaps}}
}
