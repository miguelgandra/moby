% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importData.R
\name{moby_import_schema}
\alias{moby_import_schema}
\title{Canonical column names for the moby import functions}
\description{
The import functions \code{\link[=importDetections]{importDetections()}}, \code{\link[=importTags]{importTags()}} and \code{\link[=importDeployments]{importDeployments()}}
read a raw export and rename its columns onto a small, fixed set of \emph{canonical} field names, so the
rest of moby always sees the same layout whatever the source system was. The \code{col.map} argument is
how you declare that mapping for a non-standard file: a named list of
\code{canonical_field = "your column name"}. For the built-in \code{source} presets (\code{"vue"}, \code{"glatos"},
\code{"otn"}, \code{"etn"}, ...) the mapping is filled in for you, so \code{col.map} only needs the fields a preset
missed.

Column matching is \emph{tolerant}: names are compared ignoring case, spaces and punctuation, so
\code{"Station Name"}, \code{"station.name"} and \code{"STATION_NAME"} all match the canonical \code{station}. A field
may list several candidate names, and the first one present is used:
\code{list(lon = c("Longitude", "deploy_longitude"))}.

Only a handful of fields are \strong{required}; every other field is optional and is simply absent
downstream if you do not supply it (functions that need a missing field say so when you call them).
}
\section{Detection fields — \code{importDetections()}}{
\tabular{lll}{
   Field \tab Description \tab Required \cr
   \code{datetime} \tab Detection timestamp, parsed to POSIXct (see \code{datetime.format}) \tab \strong{yes} \cr
   \code{transmitter} \tab Full tag/transmitter code — the key used to join tag metadata \tab \strong{yes} (or \code{ID}) \cr
   \code{ID} \tab Animal identifier \tab no — set from \code{transmitter} if absent \cr
   \code{transmitter_codespace} \tab Code space; joined as \code{codespace-transmitter} when both are present \tab no \cr
   \code{transmitter_name} \tab Human-readable tag name \tab no \cr
   \code{receiver} \tab Receiver serial / ID \tab no \cr
   \code{station} \tab Receiver station name \tab no \cr
   \code{lon}, \code{lat} \tab Coordinates (coerced to numeric) \tab no \cr
   \code{sensor_value}, \code{sensor_unit} \tab Sensor reading and its unit \tab no \cr
}


If \code{ID} is absent it is initialised from \code{transmitter} (with a message); assign true animal IDs
afterwards with \code{\link[=matchTags]{matchTags()}}.
}

\section{Tag fields — \code{importTags()}}{
\tabular{lll}{
   Field \tab Description \tab Required \cr
   \code{transmitter} \tab Full tag code — the key joined to detections \tab \strong{yes} \cr
   \code{ID} \tab Animal identifier \tab no — derived from \code{serial}/\code{transmitter} if absent \cr
   \code{serial} \tab Tag serial number \tab no \cr
   \code{tagging_date} \tab Release / tagging date (POSIXct) \tab no \cr
   \code{nominal_delay} \tab Transmitter mean delay, seconds (scales the min_lag filter) \tab no \cr
   \code{min_delay}, \code{max_delay} \tab Delay range; \code{nominal_delay} is set to their midpoint when it is absent \tab no \cr
   \code{species}, \code{sex}, \code{length}, \code{weight}, \code{tagging_location} \tab Biometrics / metadata \tab no \cr
}
}

\section{Deployment fields — \code{importDeployments()}}{
\tabular{lll}{
   Field \tab Description \tab Required \cr
   \code{receiver} \tab Receiver serial / ID \tab \strong{yes} \cr
   \code{station} \tab Station name \tab \strong{yes} \cr
   \code{deploy} \tab Deployment date-time (POSIXct) \tab \strong{yes} \cr
   \code{recover} \tab Recovery date-time (POSIXct); open (ongoing) deployments are handled downstream \tab no \cr
   \code{lon}, \code{lat} \tab Receiver coordinates (numeric) \tab no \cr
   \code{depth} \tab Station / bottom depth \tab no \cr
}
}

\section{What happens when an optional field is omitted}{

\itemize{
\item No \code{ID} — initialised from \code{transmitter}; attach real animal IDs with \code{\link[=matchTags]{matchTags()}}.
\item No \code{station} / \code{lon} / \code{lat}, but a \code{receiver} is present — \code{\link[=matchDeployments]{matchDeployments()}} can
back-fill them from the deployment log.
\item No \code{receiver} — deployment matching (\code{\link[=matchDeployments]{matchDeployments()}}, \code{\link[=checkDeployments]{checkDeployments()}}) is
unavailable.
\item No coordinates at all — the import still succeeds, but spatial functions (COAs, maps, step
distances) error when you later call them.
\item No \code{nominal_delay} (and no \code{min_delay}/\code{max_delay}) — the min_lag false-detection filter in
\code{\link[=filterDetections]{filterDetections()}} stays off unless you set \code{min.lag.threshold} (see that function's
\sQuote{Isolation-based filtering} section).
}
}

\section{Which function do I use?}{

The import functions and \code{\link[=as_moby]{as_moby()}} do different jobs and are normally used \strong{in sequence}, not
as alternatives:\tabular{lll}{
    \tab \code{\link[=importDetections]{importDetections()}} etc. \tab \code{\link[=as_moby]{as_moby()}} \cr
   \strong{Job} \tab \emph{Reshape} a raw export into moby's canonical layout \tab \emph{Declare} a tidy table as a \code{mobyData} \cr
   \strong{Does} \tab renames columns, parses date-times, derives \code{ID} \tab records which column plays which role; attaches metadata \cr
   \strong{Column names} \tab replaced by canonical names \tab kept as they are (\code{id.col = "animal_id"} is fine) \cr
   \strong{Carries} \tab nothing — returns a plain data frame \tab \code{id.groups}, \code{tagging.dates}, \code{nominal.delay}, CRS, land layer \cr
   \strong{Use when} \tab data comes from a receiver system, or its columns/dates need fixing \tab your table is already tidy, or you are attaching study metadata \cr
}


Roughly: importing is to \code{\link[=as_moby]{as_moby()}} what \code{read.csv()} is to \code{data.frame()} — a reader, then a
constructor. The usual pipeline runs both:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{det <- importDetections(file, source = "vue")      # 1. reshape -> data frame
det <- matchTags(det, tags)                        # 2. join    -> data frame
det <- as_moby(det, tags = tags,                   # 3. declare -> mobyData
               id.groups = groups, epsg.code = 3395, land.shape = coast)
}\if{html}{\out{</div>}}

If you \strong{already have a tidy data frame in R} (your own column names, date-times already parsed),
skip the importers and call \code{\link[=as_moby]{as_moby()}} directly — it keeps your names and can carry the full
metadata. Reach for \code{source = "generic"} only when you also want the reshaping: canonical renaming,
date parsing and \code{ID} derivation.
}

\section{Worked \code{col.map} examples}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{# 1. A non-standard VUE export where the transmitter is split across two columns
importDetections(file, source = "vue",
                 col.map = list(transmitter_codespace = "Code.Space",
                                transmitter = "ID", receiver = "Receiver.Name"))

# 2. A GLATOS data frame already loaded in R — the preset handles it, no col.map needed
importDetections(glatos_df, source = "glatos")

# 3. A plain generic CSV with your own column names
importDetections(file, source = "generic",
                 col.map = list(ID = "animal", datetime = "time_utc", station = "site",
                                lon = "x", lat = "y", transmitter = "tag"))

# 4. A tag database that stores a delay RANGE rather than a nominal delay
importTags(file, source = "generic",
           col.map = list(transmitter = "Codespace", tagging_date = "Tagdeployed",
                          min_delay = "Minoff_sec", max_delay = "Maxoff_sec"))
}\if{html}{\out{</div>}}
}

\seealso{
\code{\link[=importDetections]{importDetections()}}, \code{\link[=importTags]{importTags()}}, \code{\link[=importDeployments]{importDeployments()}}, \code{\link[=matchTags]{matchTags()}},
\code{\link[=matchDeployments]{matchDeployments()}}, \code{\link[=as_moby]{as_moby()}}
}
