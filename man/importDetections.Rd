% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importData.R
\name{importDetections}
\alias{importDetections}
\title{Import and harmonise acoustic detection data}
\usage{
importDetections(
  x,
  source = c("vue", "vdat", "glatos", "otn", "etn", "generic"),
  tz = "UTC",
  col.map = NULL,
  datetime.format = NULL,
  keep.extra = FALSE
)
}
\arguments{
\item{x}{A path to a \code{.csv} (or \code{.xlsx}) detection file, or a data frame already loaded in
R (e.g. the output of \code{etn::get_acoustic_detections()} or \code{glatos::read_glatos_detections()}).}

\item{source}{One of \code{"vue"}, \code{"vdat"}, \code{"glatos"}, \code{"otn"}, \code{"etn"} or \code{"generic"}.
For \code{"generic"}, supply \code{col.map}.}

\item{tz}{Time zone used to parse date-times. Defaults to \code{"UTC"} (the convention for
GLATOS/OTN/ETN); set explicitly for VUE/VDAT exports recorded in another zone.}

\item{col.map}{Optional named list mapping canonical fields (\code{datetime}, \code{transmitter},
\code{receiver}, \code{station}, \code{lon}, \code{lat}, \code{ID}, \code{sensor_value}, \code{sensor_unit}, ...) to the column
name(s) in \code{x}. Merged over (and overriding) the chosen \code{source} preset.}

\item{datetime.format}{Optional explicit \code{strptime} format for the datetime column; if
\code{NULL}, common layouts are auto-detected.}

\item{keep.extra}{Logical; retain source columns that were not mapped to a canonical field.
Defaults to \code{FALSE}.}
}
\value{
A \code{\link{mobyData}} object with harmonised columns
(\code{ID}, \code{datetime}, \code{transmitter}, \code{receiver}, \code{station}, \code{lon}, \code{lat}, ...). When the source
has no animal identifier, \code{ID} is initialised from \code{transmitter} (assign true animal IDs
later by joining tag metadata).
}
\description{
Reads acoustic-telemetry detections from a range of common sources and
harmonises them into a single consistent schema, returning a \code{\link{mobyData}} object
ready for the rest of the \code{moby} workflow. Supported sources are Innovasea/VEMCO VUE
exports, Innovasea VDAT/Fathom \code{DET.csv} files, and detection extracts from the GLATOS,
OTN and ETN (\code{etn} package) systems. A \code{generic} mode plus a user-supplied \code{col.map}
handle non-standard layouts.
}
\examples{
# harmonise a generic-layout detection CSV via an explicit column map
csv <- system.file("extdata", "rays_detections.csv", package = "moby")
det <- importDetections(csv, source = "generic",
                        col.map = list(ID = "animal_id", datetime = "timestamp",
                                       station = "station_name", lon = "deploy_longitude",
                                       lat = "deploy_latitude", receiver = "receiver_id",
                                       transmitter = "transmitter"))
head(det)

}
\seealso{
\code{\link{importDeployments}}, \code{\link{checkDeployments}}, \code{\link{as_moby}}
}
