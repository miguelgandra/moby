% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculateLandDists.R
\name{calculateLandDists}
\alias{calculateLandDists}
\title{Calculate distance to nearest shore/land feature and related movement metrics.}
\usage{
calculateLandDists(
  data,
  land.shape,
  epsg.code = NULL,
  mov.threshold = 0.5,
  id.col = NULL,
  lon.col = NULL,
  lat.col = NULL,
  dist.col = NULL,
  grid.resolution = 100
)
}
\arguments{
\item{data}{A data frame containing animal positions.}

\item{land.shape}{An sf object representing landmass.}

\item{epsg.code}{Integer. The projected EPSG code (must use meters). If not supplied,
it is taken from the \code{mobyData} metadata (see \code{\link{as_moby}}); a projected
(metric) CRS is required.}

\item{mov.threshold}{Numeric (0-1). Proportion of movement perpendicular to
shore required for inshore/offshore classification.}

\item{id.col}{Name of the column containing animal IDs. Defaults to \code{"ID"}.}

\item{lon.col}{Name of the column containing longitude (or projected x) values. Defaults to \code{"lon"}.}

\item{lat.col}{Name of the column containing latitude (or projected y) values. Defaults to \code{"lat"}.}

\item{dist.col}{Optional. Column name for step length (meters).}

\item{grid.resolution}{Numeric. Resolution of the distance raster in meters.}
}
\value{
A data frame with added spatial and movement columns.
}
\description{
This function calculates the distance to the nearest coastline or land feature for each
animal position. Instead of computing pairwise distances between all points and coastline vertices,
it uses a rasterised land mask and a pre-computed distance-to-land raster, providing major gains in
speed and memory efficiency for large datasets. If step lengths between consecutive positions are
supplied, the function also quantifies movement direction relative to land (inshore, offshore,
alongshore).
}
\examples{
\donttest{
data(rays)
# a small land polygon spanning the study area
land <- sf::st_sf(geometry = sf::st_sfc(sf::st_polygon(list(rbind(
  c(-9.05, 38.49), c(-8.90, 38.49), c(-8.90, 38.43),
  c(-9.05, 38.49)))), crs = 4326))
rays_land <- calculateLandDists(rays, land.shape = land)
head(rays_land$land_dist)
}

}
