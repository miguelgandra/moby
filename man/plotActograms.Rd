% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotActograms.R
\name{plotActograms}
\alias{plotActograms}
\title{Actogram plot (per-individual diel activity)}
\usage{
plotActograms(
  data,
  id.col = NULL,
  datetime.col = NULL,
  tagging.dates = NULL,
  tag.durations = NULL,
  id.groups = NULL,
  discard.missing = TRUE,
  color.by = NULL,
  color.pal = NULL,
  sunriset.coords = NULL,
  diel.lines = 2,
  solar.depth = 18,
  date.interval = "auto",
  date.format = NULL,
  date.start = 1,
  pch = 16,
  pt.cex = 1.6,
  alpha = 1,
  highlight.isolated = TRUE,
  background.color = "grey96",
  grid = FALSE,
  grid.color = "white",
  main = NULL,
  legend = TRUE,
  legend.cols = NULL,
  ncol = NULL,
  cex = 1,
  file = NULL,
  width = NULL,
  height = NULL,
  res = 300,
  ...
)
}
\arguments{
\item{data}{A data frame containing animal detections.}

\item{id.col}{Name of the column containing animal IDs. Defaults to \code{"ID"}.}

\item{datetime.col}{Name of the column containing date-times in POSIXct format. Defaults to \code{"datetime"}.}

\item{tagging.dates}{A POSIXct vector of tagging/release dates (single value or named by ID).
Inherited from the \code{mobyData} metadata when available.}

\item{tag.durations}{Optional numeric vector of estimated tag battery durations (in days), used to
mark each animal's estimated tag-expiry date. Either a single value (applied to all IDs) or a
named vector matching the IDs in \code{id.col}.}

\item{id.groups}{Optional named list of ID groups used to visually aggregate animals belonging to
the same class (e.g. species, sex or age). Each element is a vector of IDs in that group.}

\item{discard.missing}{Logical. If TRUE (default), animals without detections are omitted; if
FALSE, an empty panel is drawn for each.}

\item{color.by}{Name of the column used to colour-code detections (e.g. station or habitat). If
NULL, all detections are drawn in a single colour and no colour legend is shown.}

\item{color.pal}{Colours used to plot detections, one per \code{color.by} level. May be a plain vector
(matched to the levels by position) or a named vector (matched by name). If NULL, a colourblind-
safe palette is used (Okabe-Ito for up to 8 levels, HCL "Dark 3" beyond).}

\item{sunriset.coords}{Longitude/latitude (length-2 numeric, matrix or \code{SpatialPoints}) used for
the diel-phase times. Required only when \code{diel.lines > 0}.}

\item{diel.lines}{Number of diel-boundary lines to overlay: 0 (none), 2 (sunrise/sunset) or 4
(dawn, sunrise, sunset, dusk). Defaults to 2.}

\item{solar.depth}{Sun angle below the horizon (degrees) defining twilight. Defaults to 18.}

\item{date.interval}{Controls the x-axis date labels. Either \code{"auto"} (default), which generates
calendar-aligned "pretty" breaks appropriate to the temporal span, or a numeric value selecting
every n-th formatted date (manual mode; used with \code{date.start} and \code{date.format}).}

\item{date.format}{A date format (\code{\link[base]{strftime}}) for the x-axis labels. If NULL
(default), it is chosen automatically in \code{"auto"} mode and falls back to \code{"\%b/\%y"} in manual mode.}

\item{date.start}{Integer controlling label placement. In manual mode, the index of the first
displayed date. In \code{"auto"} mode, a phase/offset that re-anchors the automatically chosen labels
within each period (e.g. for yearly labels it selects the month). Defaults to 1.}

\item{pch}{Plotting symbol for detections. Defaults to 16 (filled circle).}

\item{pt.cex}{Expansion factor for the detection points. Defaults to 1.6.}

\item{alpha}{Opacity of the detection points, from 0 (transparent) to 1 (opaque). Defaults to 1.}

\item{highlight.isolated}{Logical. If TRUE (default) and \code{color.by} is set, isolated detections are
brought forward (plotted on top) so they are not hidden behind denser point clusters.}

\item{background.color}{Background colour of each panel. Defaults to "grey96".}

\item{grid}{Logical; draw faint hour/date guide lines. Defaults to FALSE.}

\item{grid.color}{Colour of the grid lines. Defaults to "white".}

\item{main}{Optional overall title above the whole panel.}

\item{legend}{Logical. If TRUE (default) and \code{color.by} is set, a colour legend is drawn.}

\item{legend.cols}{Integer number of columns for the \code{color.by} legend. If NULL (default), chosen
automatically.}

\item{ncol}{Number of columns in the panel grid. If NULL (default), set automatically (1 for a
single individual, 2 otherwise).}

\item{cex}{Global expansion factor applied to all plot text. Point size is controlled separately
via \code{pt.cex}. Defaults to 1.}

\item{file}{Optional output file. If \code{NULL} (the default), the figure is drawn on the current
graphics device - the usual interactive behaviour. If a file path is supplied, moby opens a
graphics device chosen from the file extension (\code{.pdf}, \code{.svg}, \code{.png}, \code{.jpg}/\code{.jpeg},
\code{.tif}/\code{.tiff} or \code{.bmp}), draws the figure to it, and closes the device automatically (also if
an error occurs). For multi-page or batch workflows, keep \code{file = NULL} and manage the device
yourself (e.g. \verb{pdf(...); plot...(); dev.off()}).}

\item{width, height}{Output size in inches. Used \emph{only} when \code{file} is supplied. If \code{NULL}
(default), a size is derived from the figure's structure (e.g. the number of individuals or
panels). These defaults are sensible starting points, \emph{not} guarantees: dense or unusual figures
may still need you to set \code{width}/\code{height} explicitly. The two can be set independently.}

\item{res}{Resolution in pixels per inch, for raster formats only (\code{.png}, \code{.jpg}, \code{.tif},
\code{.bmp}); ignored for vector formats (\code{.pdf}, \code{.svg}). Used only when \code{file} is supplied. Defaults
to 300.}

\item{...}{Further graphical parameters passed to \code{\link[graphics]{points}}.}
}
\value{
Called for its side effect: it generates actogram panel(s).
}
\description{
Produces an actogram: for each tagged individual, detections are plotted by
time-of-day (y) across the monitoring period (x), so diel patterns and their seasonal shift are
visible per animal. It is the raw, per-individual counterpart of \code{\link{plotChronogram}}
(which aggregates the population into a single panel). Detections can be coloured by any variable
(e.g. receiver or habitat) via \code{color.by}, animals can be grouped with \code{id.groups}, the diel
cycle (sunrise/sunset, optionally dawn/dusk) is overlaid, and each panel marks the release date
and, when \code{tag.durations} are supplied, the estimated tag-expiry date.
}
\details{
Panels are laid out in the order of \code{id.groups} (and, within a group, in the factor-level
order of \code{id.col}); to reorder, reorder the \code{id.col} factor levels or the IDs within \code{id.groups}.

Date-axis labels are, by default, chosen automatically to suit the temporal span of the data (from
a few days to several years), with calendar-aligned breaks. Supplying a numeric \code{date.interval}
switches to manual mode, where every n-th formatted date (per \code{date.format}, starting at
\code{date.start}) is labelled. The timezone for the time-of-day axis is taken from the data.
}
\examples{
# per-individual actograms with the diel cycle overlaid
# (coordinates are required to compute sunrise/sunset times)
plotActograms(rays, sunriset.coords = c(-9, 38.4))
}
