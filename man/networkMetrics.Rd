% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/networkMetrics.R
\name{networkMetrics}
\alias{networkMetrics}
\title{Compute node- and network-level metrics for a moby network}
\usage{
networkMetrics(network, weight = NULL, community = TRUE)
}
\arguments{
\item{network}{A \code{\link{mobyNetwork}} object (type \code{"association"} or \code{"movement"}).}

\item{weight}{Optional name of the edge column to use as weight. Defaults to \code{"association"}
(association networks) or \code{"n_movements"} (movement networks).}

\item{community}{Logical; run weighted community detection (walktrap) and report modularity
and community membership. Defaults to TRUE.}
}
\value{
An object of class \code{mobyNetworkMetrics}: a list with
\item{nodes}{A data frame of per-node metrics (one row per node and group/subset). For
association networks: \code{degree}, \code{strength}, \code{betweenness}, \code{eigenvector}, \code{clustering},
\code{community}. For movement networks: \code{in_degree}, \code{out_degree}, \code{in_strength}, \code{out_strength},
\code{betweenness}, \code{community}.}
\item{network}{A data frame of network-level metrics per group/subset (\code{n_nodes}, \code{n_edges},
\code{density}, \code{mean_strength}, \code{modularity}, \code{n_communities}, plus \code{transitivity} for
association networks or \code{reciprocity} for movement networks).}
}
\description{
Computes a consistent set of graph-theoretic metrics for either an association
(individual co-occurrence) network or a movement (location-transition) network produced by
\code{\link{calculateAssociations}} / \code{\link{calculateTransitions}}. Metrics are computed
per group/subset when the network carries one.

Edge weights are interpreted as connection strengths (association index for association
networks; number of movements for movement networks). For shortest-path metrics
(betweenness) weights are internally inverted, so that stronger ties act as shorter distances
and high-betweenness nodes identify brokers / movement corridors.
}
\examples{
data(rays)
trans <- calculateTransitions(rays, spatial.col = "station")
# node- and network-level graph metrics
metrics <- networkMetrics(trans)
metrics
head(metrics$nodes)

}
\seealso{
\code{\link{calculateAssociations}}, \code{\link{calculateTransitions}}
}
