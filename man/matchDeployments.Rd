% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkDeployments.R
\name{matchDeployments}
\alias{matchDeployments}
\title{Match detections to receiver deployments and back-fill metadata}
\usage{
matchDeployments(
  detections,
  deployments,
  datetime.col = NULL,
  station.col = NULL,
  lon.col = NULL,
  lat.col = NULL,
  deployment.station.col = "station",
  deployment.lon.col = "lon",
  deployment.lat.col = "lat",
  deployment.deploy.col = "deploy",
  deployment.recover.col = "recover",
  coord.tolerance = 500,
  fill.coords = TRUE,
  fill.station = TRUE,
  drop.unmatched = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{detections}{A detection dataset (\code{mobyData} or data frame) with a \code{receiver} column.}

\item{deployments}{A receiver-deployment data frame from \code{\link{importDeployments}}
(\code{receiver} plus station, coordinates and deploy/recover date-times; column names resolved via the
\verb{deployment.*} arguments).}

\item{datetime.col, station.col, lon.col, lat.col}{Column names in the \strong{detection} dataset
(\code{detections}), resolved from its \code{mobyData} metadata or canonical defaults when \code{NULL}. Bare
\verb{*.col} arguments always refer to the detections; the deployment log's columns use the
\verb{deployment.*} arguments.}

\item{deployment.station.col, deployment.lon.col, deployment.lat.col}{Names of the station,
longitude and latitude columns in the receiver-deployment log (\code{deployments}). Default to the
canonical \code{"station"}/\code{"lon"}/\code{"lat"} produced by \code{\link{importDeployments}}; set them when a
hand-made log uses other names (e.g. \code{deployment.lon.col = "Longitude"}). The \code{deployment.} prefix
marks these as deployment-log columns, keeping them distinct from the bare \verb{*.col} arguments,
which always refer to the detection dataset. The \code{receiver} column is the canonical join key and
is always taken as-is.}

\item{deployment.deploy.col, deployment.recover.col}{Names of the deployment and recovery
date-time columns in the receiver-deployment log (\code{deployments}). Default to the canonical
\code{"deploy"}/\code{"recover"} produced by \code{\link{importDeployments}}; set them when a hand-made log
uses other names (e.g. \code{deployment.deploy.col = "deploy_date"}). The \code{deployment.} prefix marks
these as deployment-log columns, keeping them distinct from the bare \verb{*.col} arguments, which
always refer to the detection dataset.}

\item{coord.tolerance}{Numeric. Distance (metres for geographic coordinates) above which a
detection's own coordinates are flagged as disagreeing with the deployment metadata. The
metadata coordinates are treated as authoritative for back-filling. Defaults to 500.}

\item{fill.coords, fill.station}{Logical; back-fill missing detection coordinates / station
names from the matched deployment. Default TRUE.}

\item{drop.unmatched}{Logical; drop detections that fall outside every deployment window.
Defaults to FALSE (they are retained and flagged).}

\item{verbose}{Logical; print a short summary. Defaults to TRUE.}
}
\value{
A \code{\link{mobyData}} object (the detections) with back-filled \code{lon}/\code{lat}/station
where missing, plus two logical flag columns: \code{deployment_matched} (detection fell within a
valid window) and \code{coord_mismatch} (own vs metadata coordinates differ beyond
\code{coord.tolerance}).
}
\description{
Joins a receiver-deployment log (see \code{\link{importDeployments}}) to a
detection dataset, matching each detection to the deployment window
(\code{receiver} + time within \verb{[deploy, recover]}) it falls in, and back-filling missing
coordinates and station names from the receiver log. When a receiver was moved between
stations and a detection matches more than one window, the window whose station name agrees
with the detection is preferred. Coordinate disagreements between the detection export and the
metadata (a common VUE artefact) are flagged. This applies the corrections for the issues
reported by \code{\link{checkDeployments}}, and is a natural follow-up to it.
}
\examples{
# match detections to deployment windows, back-filling station/coordinates
matched <- matchDeployments(rays_detections, rays_deployments, station.col = "station")
table(matched$deployment_matched)

}
\seealso{
\code{\link{checkDeployments}}, \code{\link{importDeployments}}, \code{\link{importDetections}}
}
