% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotAssociationMatrix.R
\name{plotAssociationMatrix}
\alias{plotAssociationMatrix}
\title{Plot an association significance / overlap matrix}
\usage{
plotAssociationMatrix(
  random.results,
  type = c("significance", "mean overlap"),
  color.pal = NULL,
  sort.by = NULL,
  sort.by.title = NULL,
  full.scale = FALSE,
  discard.missing = TRUE,
  main = NULL,
  cex = 1,
  file = NULL,
  width = NULL,
  height = NULL,
  res = 300
)
}
\arguments{
\item{random.results}{The null-model output of \code{\link{randomizeAssociations}}.}

\item{type}{One of \code{"significance"} (default; +/-/ns from the FDR-adjusted permutation test) or
\code{"mean overlap"} (the mean pairwise overlap percentage).}

\item{color.pal}{Colour palette. For \code{"significance"}, a length-3 vector for
positive / negative / non-significant; for \code{"mean overlap"}, a continuous gradient. If NULL,
colourblind-safe defaults are used.}

\item{sort.by}{Optional numeric/integer/factor/POSIXct vector (length = number of IDs) used to
reorder rows and columns (e.g. by size). Values are shown in a header row/column: numbers to a
common number of decimal places, factors as their labels (ordered by level) and dates formatted
(ordered chronologically).}

\item{sort.by.title}{Header label for the \code{sort.by} metric. If omitted, it is inferred from a
\code{data$column} expression.}

\item{full.scale}{Logical; for \code{"mean overlap"}, map the colour ramp over the fixed 0-100\% domain
instead of the observed range. Defaults to FALSE.}

\item{discard.missing}{Logical; drop rows/columns with no valid pairwise interactions. Defaults to TRUE.}

\item{main}{Optional overall title.}

\item{cex}{Global expansion factor for all text (cell labels and legend). Defaults to 1.}

\item{file}{Optional output file. If \code{NULL} (the default), the figure is drawn on the current
graphics device - the usual interactive behaviour. If a file path is supplied, moby opens a
graphics device chosen from the file extension (\code{.pdf}, \code{.svg}, \code{.png}, \code{.jpg}/\code{.jpeg},
\code{.tif}/\code{.tiff} or \code{.bmp}), draws the figure to it, and closes the device automatically (also if
an error occurs). For multi-page or batch workflows, keep \code{file = NULL} and manage the device
yourself (e.g. \verb{pdf(...); plot...(); dev.off()}).}

\item{width, height}{Output size in inches. Used \emph{only} when \code{file} is supplied. If \code{NULL}
(default), a size is derived from the figure's structure (e.g. the number of individuals or
panels). These defaults are sensible starting points, \emph{not} guarantees: dense or unusual figures
may still need you to set \code{width}/\code{height} explicitly. The two can be set independently.}

\item{res}{Resolution in pixels per inch, for raster formats only (\code{.png}, \code{.jpg}, \code{.tif},
\code{.bmp}); ignored for vector formats (\code{.pdf}, \code{.svg}). Used only when \code{file} is supplied. Defaults
to 300.}
}
\value{
Invisibly, the id x id matrix of plotted values (significance labels or overlap percentages).
}
\description{
Draws an id x id matrix of pairwise association results from a
\code{\link{randomizeAssociations}} null model - coloured either by permutation significance
(positive / negative / non-significant) or by mean overlap percentage. Individuals can be sorted by
a supplied metric (e.g. body size).
}
\examples{
\donttest{
# Significance / overlap matrix from a permutation null model
wide  <- createWideTable(rays, value.col = "station")
assoc <- calculateAssociations(wide)
rand  <- randomizeAssociations(wide, assoc, iterations = 100, random.seed = 1)
plotAssociationMatrix(rand)
plotAssociationMatrix(rand, type = "mean overlap")
}
}
\seealso{
\code{\link{calculateAssociations}}, \code{\link{randomizeAssociations}}, \code{\link{plotAssociations}}
}
