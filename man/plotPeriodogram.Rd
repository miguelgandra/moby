% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotPeriodogram.R
\name{plotPeriodogram}
\alias{plotPeriodogram}
\title{Plot detection periodograms (dominant rhythms)}
\usage{
plotPeriodogram(
  data,
  id.col = NULL,
  timebin.col = NULL,
  id.groups = NULL,
  type = "detections",
  method = c("fft", "lomb"),
  detrend = c("none", "linear", "diff"),
  gap.handling = c("zero", "mean", "locf", "interpolate"),
  period.range = NULL,
  axis.periods = c(48, 24, 12, 8, 6),
  highlight.bands = TRUE,
  min.days = 10,
  shared.scale = TRUE,
  color.pal = NULL,
  background.color = "grey96",
  ncol = NULL,
  cex = 1,
  file = NULL,
  width = NULL,
  height = NULL,
  res = 300
)
}
\arguments{
\item{data}{A \code{mobyData} or data frame of binned detections (must contain a \code{detections} column).}

\item{id.col}{Name of the column containing animal IDs. Defaults to \code{"ID"}.}

\item{timebin.col}{Name of the column containing time bins (in POSIXct format). Defaults to \code{"timebin"}.}

\item{id.groups}{Optional named list of ID groups (one block of panels each).}

\item{type}{Response analysed: \code{"detections"} (counts) or \code{"presences"} (binary per bin).}

\item{method}{\code{"fft"} (default; periodogram on the gap-filled regular grid) or \code{"lomb"}
(Lomb-Scargle on the observed samples; needs the \pkg{lomb} package).}

\item{detrend}{One of \code{"none"} (default; demean), \code{"linear"} (remove OLS trend), or \code{"diff"}
(first difference - high-pass, use with care).}

\item{gap.handling}{How internal gaps in the regular grid are filled for \code{method = "fft"}: \code{"zero"}
(absence; default), \code{"mean"}, \code{"locf"} or \code{"interpolate"}.}

\item{period.range}{Optional length-2 numeric (hours) giving the period range to display and search
for the dominant peak. If NULL (default), spans ~2 sampling intervals to twice the largest
\code{axis.periods} value.}

\item{axis.periods}{Reference periods (hours) marked on the x-axis. Defaults to c(48, 24, 12, 8, 6).}

\item{highlight.bands}{Logical; shade the tidal (~6-12.4 h) and diel (~24 h) bands. Defaults to TRUE.}

\item{min.days}{Discard individuals detected on fewer than this many distinct days. Defaults to 10.}

\item{shared.scale}{Logical; share the y-axis (spectral power) across panels. Defaults to TRUE.}

\item{color.pal}{Colour for the spectrum. If NULL, a colourblind-safe colour is used.}

\item{background.color}{Panel background colour. Defaults to "grey96".}

\item{ncol}{Number of panel columns. If NULL, set from the number of individuals.}

\item{cex}{Global expansion factor for all plot text. Defaults to 1.}

\item{file}{Optional output file. If \code{NULL} (the default), the figure is drawn on the current
graphics device - the usual interactive behaviour. If a file path is supplied, moby opens a
graphics device chosen from the file extension (\code{.pdf}, \code{.svg}, \code{.png}, \code{.jpg}/\code{.jpeg},
\code{.tif}/\code{.tiff} or \code{.bmp}), draws the figure to it, and closes the device automatically (also if
an error occurs). For multi-page or batch workflows, keep \code{file = NULL} and manage the device
yourself (e.g. \verb{pdf(...); plot...(); dev.off()}).}

\item{width, height}{Output size in inches. Used \emph{only} when \code{file} is supplied. If \code{NULL}
(default), a size is derived from the figure's structure (e.g. the number of individuals or
panels). These defaults are sensible starting points, \emph{not} guarantees: dense or unusual figures
may still need you to set \code{width}/\code{height} explicitly. The two can be set independently.}

\item{res}{Resolution in pixels per inch, for raster formats only (\code{.png}, \code{.jpg}, \code{.tif},
\code{.bmp}); ignored for vector formats (\code{.pdf}, \code{.svg}). Used only when \code{file} is supplied. Defaults
to 300.}
}
\value{
Invisibly, a tidy data frame with one row per individual: the dominant period (h), its
power, the false-alarm probability (\code{method = "lomb"} only), and whether it falls in the tidal or
diel band.
}
\description{
Computes and plots a periodogram per individual - spectral power against period - to
reveal dominant cyclic patterns in a detection time series, such as tidal (~6-12 h), diel (24 h)
or lunar rhythms in habitat use. One small panel is drawn per animal, with the tidal and diel
bands shaded and the dominant period marked. It is the global-spectrum complement of
\code{\link{plotScalogram}} (which resolves how those rhythms change through time).
}
\details{
The detection series is first laid on a complete, regularly-sampled time grid (via the
shared signal builder), so temporal gaps are \emph{represented} rather than deleted - the ordinary FFT
assumes even sampling, and dropping absent bins corrupts the period axis. By default \code{method = "fft"} computes a raw periodogram (\code{\link[stats]{spec.pgram}}) on that grid, treating an
unobserved bin as absence (0). For data where absence-as-zero is not appropriate, \code{method = "lomb"} runs a Lomb-Scargle periodogram on the observed (time, value) pairs (requires the
\pkg{lomb} package) and reports an analytic false-alarm probability for the dominant peak.

Detrending defaults to \code{"none"} (mean removal only): the common first-difference detrend is a
high-pass filter that \emph{suppresses} exactly the low-frequency diel/tidal peaks this tool looks for.
}
\examples{
# Collapse detections into hourly counts per individual, then find dominant rhythms
binned <- aggregate(list(detections = rep(1L, nrow(rays))),
                    by = list(ID = rays$ID, timebin = rays$timebin), FUN = sum)
plotPeriodogram(binned, id.col = "ID", timebin.col = "timebin")
}
\seealso{
\code{\link{plotScalogram}}, \code{\link{plotActograms}}, \code{\link{plotChronogram}}
}
