% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculateTransitions.R
\name{calculateTransitions}
\alias{calculateTransitions}
\title{Build a movement (transition) network between locations}
\usage{
calculateTransitions(
  data,
  spatial.col = NULL,
  id.col = NULL,
  datetime.col = NULL,
  id.groups = NULL,
  max.gap = 48,
  max.gap.unit = c("hours", "days", "mins", "secs")
)
}
\arguments{
\item{data}{A data frame (or \code{\link{mobyData}}) of detections.}

\item{spatial.col}{Name of the column defining the network nodes (e.g. receiver, station,
habitat, region). If \code{NULL} (default), it is taken from the \code{mobyData} station column (or the
canonical \code{"station"}); set it to build transitions between any other spatial unit.}

\item{id.col}{Name of the column containing animal IDs. Defaults to \code{"ID"}.}

\item{datetime.col}{Name of the column containing date-times in POSIXct format. Defaults to \code{"datetime"}.}

\item{id.groups}{Optional named list of ID groups; when supplied, an independent network is
built for each group (carried in the \code{group} column of the node/edge tables).}

\item{max.gap}{Maximum tolerated gap between successive detections within a single visit (passed
to \code{\link{calculateVisits}}); a longer absence ends a stay, so a later return counts as a new
visit. Defaults to 48 (hours). Use \code{Inf} to segment on location changes only.}

\item{max.gap.unit}{Units of \code{max.gap}: one of \code{"hours"} (default), \code{"days"}, \code{"mins"}, \code{"secs"}.}
}
\value{
A \code{\link{mobyNetwork}} object of type \code{"movement"}. Its rows are the directed
edges with columns: \code{group}, \code{from}, \code{to}, \code{n_movements} (number of transitions),
\code{n_individuals} (distinct individuals performing them), and \code{mean_duration_h} (mean transit time
in hours, computed over continuously-observed movements only — transits that spanned a \code{max.gap}
absence are excluded as their timing is unobserved). The node table (\code{\link{networkNodes}})
has one row per location with \code{group}, \code{site}, \code{n_detections}, \code{n_individuals}, \code{n_residence}
(number of residence events / visits), \code{mean_residence_h} (mean visit duration in hours), and
\code{lon}/\code{lat} (mean coordinates, when available). The full per-transition records (departure /
arrival times, hour, month, \code{duration_h}, and \code{crossed_gap}) are stored in the
\code{"transition_records"} attribute.
}
\description{
Builds a \strong{movement network}, in which nodes are spatial units (receivers,
stations, habitats or any user-defined spatial class) and directed edges represent transitions
\itemize{
\item i.e. individuals moving from one location to another. Edge weights summarise how many
movements occurred and how many distinct individuals performed them. This is the spatial
counterpart to \code{\link{calculateAssociations}}, and the foundation for movement-network
metrics, visualisation and (in a forthcoming release) randomisation.
}

Transitions are defined between \emph{consecutive distinct} residence events (visits) in each
individual's time-ordered sequence, where visits are segmented by \code{max.gap} (see
\code{\link{calculateVisits}}): consecutive detections at the same location are collapsed into one
visit unless separated by an absence longer than \code{max.gap}, so a transition is genuine movement
between two different nodes and a long absence is not mistaken for one continuous stay.
}
\examples{
data(rays)
# build a movement network with the receiver stations as nodes
trans <- calculateTransitions(rays, spatial.col = "station")
trans
head(networkEdges(trans))

}
\seealso{
\code{\link{calculateVisits}}, \code{\link{calculateAssociations}},
\code{\link{mobyNetwork}}, \code{\link{transitionsTable}}
}
