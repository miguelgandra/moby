% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotMovements.R
\name{plotMovements}
\alias{plotMovements}
\title{Plot a movement (transition) network on a map}
\usage{
plotMovements(
  network,
  edge.metric = c("movements", "individuals"),
  land.shape = NULL,
  coastline = getOption("moby.coastline", TRUE),
  land.color = "gray50",
  epsg.code = NULL,
  background.layer = NULL,
  background.color = "#F3F7F7",
  background.pal = NULL,
  color.nodes.by = c("detection", "group"),
  nodes.color = NULL,
  nodes.alpha = 0.8,
  nodes.size = c(0.04, 0.08),
  nodes.label = TRUE,
  nodes.label.wrap = FALSE,
  nodes.label.color = "white",
  repel.nodes = TRUE,
  repel.buffer = 1.1,
  edge.color = "darkblue",
  edge.curved = 0.5,
  edge.width = c(0.4, 3.5),
  edge.arrow.size = 0.5,
  edge.arrow.width = 1.5,
  edge.label = TRUE,
  edge.label.color = "black",
  edge.label.font = 1,
  scale.km = NULL,
  scale.pos = "bottom",
  scale.inset = c(0, 0.05),
  scale.height = 1.5,
  scale.color = "black",
  extent.factor = 1.1,
  main = NULL,
  cex = 1,
  ncol = 1,
  file = NULL,
  width = NULL,
  height = NULL,
  res = 300,
  ...
)
}
\arguments{
\item{network}{A \code{\link{mobyNetwork}} object of type \code{"movement"}, as returned by
\code{\link{calculateTransitions}}. When the network carries ID groups, a separate panel is drawn
for each group.}

\item{edge.metric}{Metric used to scale and label the edges: \code{"movements"} (number of transitions,
the default) or \code{"individuals"} (number of distinct individuals transiting between sites).}

\item{land.shape}{Optional. An \code{sf} object containing coastlines, drawn underneath the network.}

\item{coastline}{When the map is projected (an \code{epsg.code}, \code{land.shape} or \code{background.layer} is
given) and no \code{land.shape} is supplied, draw a default coastline for the extent. \code{TRUE} auto-picks
a scale; a string forces an \code{rnaturalearth} scale; \code{FALSE} draws none. Defaults to
\code{getOption("moby.coastline", TRUE)}. Requires the (Suggested) \code{rnaturalearth} (with
\code{rnaturalearthdata}) or \code{maps} package.}

\item{land.color}{Colour of land areas. Defaults to "gray50".}

\item{epsg.code}{Optional. A projected coordinate reference system (numeric EPSG code or \code{crs}
object, in metre units) used to project the node coordinates and the map layers. Required for the
scale bar when coordinates are geographic.}

\item{background.layer}{Optional. A projected raster displayed in the background (e.g. bathymetry,
temperature).}

\item{background.color}{Solid colour used for the plot background when no \code{background.layer} is
supplied. Defaults to "#F3F7F7".}

\item{background.pal}{Colour palette for the \code{background.layer} raster. If NULL (default), a
muted colourblind-safe viridis palette is used (continuous layers are drawn semi-transparent).}

\item{color.nodes.by}{How to colour the nodes: \code{"detection"} (default) uses a single colour for
all visited sites, or \code{"group"} assigns a different colour per ID group (one per panel).}

\item{nodes.color}{Colour(s) for the nodes. If NULL (default), a single blue for \code{"detection"}
mode, or a colourblind-friendly Okabe-Ito palette (one colour per group) for \code{"group"} mode.}

\item{nodes.alpha}{Numeric transparency for the nodes (0 = transparent, 1 = opaque). Defaults to 0.8.}

\item{nodes.size}{A length-2 numeric vector giving the minimum and maximum node sizes relative to
the x-axis (as proportions). Defaults to c(0.04, 0.08).}

\item{nodes.label}{Logical. If TRUE (default), node labels (site name and number of individuals)
are drawn inside the nodes.}

\item{nodes.label.wrap}{Logical. If TRUE, splits node labels into multiple lines. Defaults to FALSE.}

\item{nodes.label.color}{Font colour for node labels. Defaults to "white".}

\item{repel.nodes}{Logical. If TRUE (default), nodes are repositioned with a repulsion algorithm
to reduce overlap (requires the \code{packcircles} package).}

\item{repel.buffer}{Controls the spacing between nodes when \code{repel.nodes} is TRUE. Defaults to 1.1.}

\item{edge.color}{Colour(s) for the edges (recycled across groups). Defaults to "darkblue".}

\item{edge.curved}{Curvature of the edges. A numeric value sets the curvature (0 = straight);
TRUE means 0.5, FALSE means 0. Defaults to 0.5.}

\item{edge.width}{A length-2 numeric vector giving the minimum and maximum edge widths.
Defaults to c(0.4, 3.5).}

\item{edge.arrow.size}{Size of the arrowheads. Defaults to 0.5.}

\item{edge.arrow.width}{Width of the arrowheads. Defaults to 1.5.}

\item{edge.label}{Logical. If TRUE (default), each edge is labelled with its \code{edge.metric} value.}

\item{edge.label.color}{Colour of the edge labels. Defaults to "black".}

\item{edge.label.font}{Font for the edge labels (1 = plain, 2 = bold, 3 = italic, 4 = bold italic).
Defaults to 1.}

\item{scale.km}{Length of the scale bar, in kilometres. If NULL, it is defined automatically.
Only drawn when coordinates are projected.}

\item{scale.pos}{Position of the scale bar, specified by keyword (e.g. "bottom"). Defaults to "bottom".}

\item{scale.inset}{Controls how far the scale bar is inset from the plot edges. A single value or a
length-2 vector (x, y). Defaults to c(0, 0.05).}

\item{scale.height}{Thickness of the scale bar. Defaults to 1.5.}

\item{scale.color}{Colour of the scale-bar labels. Defaults to "black".}

\item{extent.factor}{Numeric factor to expand the plotting region around the node positions. A
value of 1 keeps the bounding box; values > 1 increase the extent. Defaults to 1.1.}

\item{main}{Optional overall title drawn above the panel grid.}

\item{cex}{Global expansion factor scaling every text element (titles, node/edge labels, scale
bar, legend). Defaults to 1.}

\item{ncol}{Number of columns in the multi-panel layout. Defaults to 1.}

\item{file}{Optional output file. If \code{NULL} (the default), the figure is drawn on the current
graphics device - the usual interactive behaviour. If a file path is supplied, moby opens a
graphics device chosen from the file extension (\code{.pdf}, \code{.svg}, \code{.png}, \code{.jpg}/\code{.jpeg},
\code{.tif}/\code{.tiff} or \code{.bmp}), draws the figure to it, and closes the device automatically (also if
an error occurs). For multi-page or batch workflows, keep \code{file = NULL} and manage the device
yourself (e.g. \verb{pdf(...); plot...(); dev.off()}).}

\item{width, height}{Output size in inches. Used \emph{only} when \code{file} is supplied. If \code{NULL}
(default), a size is derived from the figure's structure (e.g. the number of individuals or
panels). These defaults are sensible starting points, \emph{not} guarantees: dense or unusual figures
may still need you to set \code{width}/\code{height} explicitly. The two can be set independently.}

\item{res}{Resolution in pixels per inch, for raster formats only (\code{.png}, \code{.jpg}, \code{.tif},
\code{.bmp}); ignored for vector formats (\code{.pdf}, \code{.svg}). Used only when \code{file} is supplied. Defaults
to 300.}

\item{...}{Further arguments passed to \code{\link[igraph]{plot.igraph}}.}
}
\value{
Invisibly, a tidy data frame of the plotted nodes (site, group, coordinates, number of
individuals and detections). The corresponding edge table is attached as attribute \code{"edges"}.
Called mainly for its side effect (the network plot).
}
\description{
Plots a spatial movement network from a \code{\link{mobyNetwork}} object of type
"movement" (as returned by \code{\link{calculateTransitions}}). Nodes are drawn at the geographic
location of each spatial unit (receiver, station, habitat, region) and directed edges (arrows)
represent transitions between them. Node size reflects the number of individuals detected at each
location, while edge width and labels reflect either the number of movements or the number of
transiting individuals.

An optional coastline (\code{land.shape}) and a background raster (\code{background.layer}, e.g. bathymetry
or temperature) can be supplied to render the network over a projected map, complete with a scale
bar. When node coordinates are unavailable, a force-directed layout is used instead.

This function is also invoked automatically by \code{plot()} on a movement-type
\code{\link{mobyNetwork}} (see \code{\link{plot.mobyNetwork}}).
}
\examples{
# Build a station-to-station transition network, then draw it on a projected map
net <- calculateTransitions(rays, spatial.col = "station")
plotMovements(net, epsg.code = 32629, coastline = FALSE)   # coastline = TRUE draws a coastline
}
\seealso{
\code{\link{calculateTransitions}}, \code{\link{plot.mobyNetwork}},
\code{\link{networkMetrics}}, \code{\link{plotAssociations}}
}
