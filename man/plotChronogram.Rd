% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotChronogram.R
\name{plotChronogram}
\alias{plotChronogram}
\title{Chronogram (hour x date heatmap)}
\usage{
plotChronogram(
  data,
  id.col = NULL,
  timebin.col = NULL,
  station.col = NULL,
  split.by = NULL,
  metric = "detections",
  style = "points",
  color.by = NULL,
  color.pal = NULL,
  coords = NULL,
  solar.depth = 18,
  diel.lines = 4,
  shade = FALSE,
  date.interval = "auto",
  date.format = NULL,
  date.start = 1,
  pt.cex = c(0.5, 3),
  alpha = 0.7,
  background.color = "white",
  grid = TRUE,
  grid.color = "white",
  highlight.isolated = FALSE,
  shared.scale = FALSE,
  shared.dates = TRUE,
  panel.titles = NULL,
  main = NULL,
  legend = TRUE,
  legend.cols = NULL,
  ncol = NULL,
  cex = 1,
  file = NULL,
  width = NULL,
  height = NULL,
  res = 300,
  ...
)
}
\arguments{
\item{data}{A data frame of detections including a time-bin column (\code{timebin.col}).}

\item{id.col}{Name of the column containing animal IDs. Defaults to \code{"ID"}.}

\item{timebin.col}{Name of the column containing time bins (in POSIXct format). Defaults to \code{"timebin"}.}

\item{station.col}{Name of the column containing station/receiver IDs. Defaults to \code{"station"}.}

\item{split.by}{Optional column name; when supplied, a separate panel is drawn for each of its
levels (e.g. species, life stage, habitat).}

\item{metric}{The metric to plot: one of \code{"detections"} (the default), \code{"individuals"} or
\code{"co-occurrences"} (see Details for the co-occurrence definition and its caveats).}

\item{style}{\code{"points"} (metric encoded by point size) or \code{"raster"} (metric encoded by colour).}

\item{color.by}{For \code{style = "points"}, a column used to colour the points (factor or numeric).}

\item{color.pal}{Colours (or a palette function) for the points/raster. If NULL, a colourblind-safe
default is used (Okabe-Ito for categorical, viridis for continuous/raster).}

\item{coords}{Longitude/latitude (a length-2 numeric, matrix or \code{SpatialPoints}) at which
to compute diel-phase times. Required only when \code{diel.lines > 0} or \code{shade = "diel"}.}

\item{solar.depth}{Sun angle below the horizon (degrees) defining twilight. Defaults to 18.}

\item{diel.lines}{Number of diel-boundary lines to draw: 0, 2 (sunrise/sunset) or 4 (dawn,
sunrise, sunset, dusk). Defaults to 4.}

\item{shade}{Background shading for \code{style = "points"}. One of \code{"diel"} (day/night/twilight),
\code{"season"}, FALSE (none, default), or a data frame of custom periods with columns \code{start} and
\code{end} (POSIXct or coercible) and, optionally, \code{label} and \code{color} (same structure as
\code{\link{plotAbacus}}).}

\item{date.interval}{Date-axis labels: \code{"auto"} (default) for span-appropriate "pretty" breaks, or
a numeric value selecting every n-th formatted date (manual mode).}

\item{date.format}{Date format for the labels (\code{\link[base]{strftime}}). If NULL (default),
chosen automatically in \code{"auto"} mode and defaults to \code{"\%d/\%b"} in manual mode.}

\item{date.start}{In manual mode, the index of the first labelled date; in \code{"auto"} mode, a phase
offset re-anchoring the labels within each period. Defaults to 1.}

\item{pt.cex}{Length-2 numeric giving the min and max point sizes (\code{style = "points"}). Defaults
to c(0.5, 3).}

\item{alpha}{Opacity of the points, from 0 (transparent) to 1 (opaque). Defaults to 0.7.}

\item{background.color}{Background colour (\code{style = "points"}, when \code{shade} is not used).}

\item{grid}{Logical; draw faint hour/date guide lines. Defaults to TRUE.}

\item{grid.color}{Colour of the grid lines. Defaults to "white".}

\item{highlight.isolated}{Logical; for \code{style = "points"} with \code{color.by}, plot isolated colour
levels on top so they are not hidden behind denser clusters. Defaults to FALSE.}

\item{shared.scale}{Use a common metric (colour/size) scale across panels. Defaults to FALSE.}

\item{shared.dates}{Use a common date range across panels. Defaults to TRUE.}

\item{panel.titles}{Per-panel title(s). If NULL (default), titles are generated automatically (the
group name when \code{split.by} is used, otherwise the metric); a character vector overrides them
(recycled across panels); FALSE omits them.}

\item{main}{Optional overall title above the whole panel grid.}

\item{legend}{Logical; draw the legend(s). Defaults to TRUE.}

\item{legend.cols}{Number of columns for the \code{color.by} legend. If NULL, chosen automatically.}

\item{ncol}{Number of columns in the panel layout. If NULL, panels are stacked (one column).}

\item{cex}{Global expansion factor for all plot text. Defaults to 1.}

\item{file}{Optional output file. If \code{NULL} (the default), the figure is drawn on the current
graphics device - the usual interactive behaviour. If a file path is supplied, moby opens a
graphics device chosen from the file extension (\code{.pdf}, \code{.svg}, \code{.png}, \code{.jpg}/\code{.jpeg},
\code{.tif}/\code{.tiff} or \code{.bmp}), draws the figure to it, and closes the device automatically (also if
an error occurs). For multi-page or batch workflows, keep \code{file = NULL} and manage the device
yourself (e.g. \verb{pdf(...); plot...(); dev.off()}).}

\item{width, height}{Output size in inches. Used \emph{only} when \code{file} is supplied. If \code{NULL}
(default), a size is derived from the figure's structure (e.g. the number of individuals or
panels). These defaults are sensible starting points, \emph{not} guarantees: dense or unusual figures
may still need you to set \code{width}/\code{height} explicitly. The two can be set independently.}

\item{res}{Resolution in pixels per inch, for raster formats only (\code{.png}, \code{.jpg}, \code{.tif},
\code{.bmp}); ignored for vector formats (\code{.pdf}, \code{.svg}). Used only when \code{file} is supplied. Defaults
to 300.}

\item{...}{Further arguments passed to \code{\link[graphics]{image}} (raster) or
\code{\link[graphics]{points}} (points).}
}
\value{
Called for its side effect: it generates chronogram panel(s).
}
\description{
Draws a chronogram: a two-dimensional hour-of-day by date plot of a chosen metric
(number of detections, individuals or co-occurring animals) per time bin. Detections can be shown
as size-coded points (\code{style = "points"}) or colour-coded raster cells (\code{style = "raster"}), with
the diel cycle (dawn / sunrise / sunset / dusk boundaries, and optional day/night/season shading)
overlaid to reveal temporal patterns of habitat use. When \code{split.by} is supplied, a separate panel
is drawn for each group (e.g. species).
}
\details{
Date-axis labels are chosen automatically to suit the temporal span (\code{date.interval = "auto"}); a numeric \code{date.interval} reverts to manual mode, and \code{date.start} shifts the labels (a
phase offset in auto mode). For \code{style = "points"}, \code{color.by} colours points by any variable while
point size encodes the metric; \code{shade} shades the diel cycle or seasons in the background.

The \code{"co-occurrences"} metric counts the number of distinct individuals sharing the same station
within the same time bin (i.e. the local group size, restricted to cells with two or more animals).
It is therefore an exploratory descriptor whose magnitude is sensitive to the time-bin width and
the spatial resolution of \code{station.col} (coarser bins/stations inflate apparent co-occurrence) and
to detection effort; it is not corrected for the number of animals available. For quantitative
co-occurrence/association analysis (with permutation-based null models) use
\code{\link{calculateAssociations}}.
}
\note{
The right margin is sized from the measured legend width and legends are positioned in inch
units (via \code{\link[graphics]{grconvertX}} / \code{\link[graphics]{grconvertY}}), so the layout
stays consistent across datasets and graphics devices.
}
\examples{
# chronogram of detections with the diel cycle overlaid
plotChronogram(rays, coords = c(-9, 38.4))

# without the diel overlay (no coordinates required)
plotChronogram(rays, diel.lines = 0)
}
