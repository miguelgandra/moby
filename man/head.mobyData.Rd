% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mobyData.R
\name{head.mobyData}
\alias{head.mobyData}
\alias{tail.mobyData}
\title{Return the first or last rows of a \code{mobyData}}
\usage{
\method{head}{mobyData}(x, n = 6L, ...)

\method{tail}{mobyData}(x, n = 6L, ...)
}
\arguments{
\item{x}{A \code{\link{mobyData}} object.}

\item{n}{Number of rows. Defaults to 6, as for \code{utils::head}.}

\item{...}{Further arguments passed to \code{utils::head} / \code{utils::tail}.}
}
\value{
A \code{data.frame} of the first (or last) \code{n} rows.
}
\description{
\code{head()} and \code{tail()} behave as they do for any data frame: they return the first
(or last) \code{n} rows of the detection table for inspection. They return a plain \code{data.frame}, so
the rows print as rows - the study metadata summary is what \code{print()} on the \code{mobyData}
itself is for.

To subset a dataset and \emph{keep} it a \code{mobyData} (metadata and all), use \code{[} instead:
\code{x[1:100, ]}.
}
\examples{
data(rays)
head(rays, 3)      # rows, like any data frame
dim(rays[1:100, ]) # '[' keeps the mobyData class and its metadata

}
\seealso{
\code{\link{as_moby}}, \code{\link{mobyMeta}}
}
