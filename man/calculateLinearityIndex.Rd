% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculateLinearityIndex.R
\name{calculateLinearityIndex}
\alias{calculateLinearityIndex}
\title{Calculate the linearity index of individual movements}
\usage{
calculateLinearityIndex(
  data,
  land.shape = NULL,
  epsg.code = NULL,
  id.col = NULL,
  timebin.col = NULL,
  lon.col = NULL,
  lat.col = NULL,
  dist.col = "dist_m",
  ...
)
}
\arguments{
\item{data}{A data frame (or \code{\link{mobyData}}) of binned detections with stepwise
distances, as returned by \code{\link{calculateStepDistances}}.}

\item{land.shape}{Optional. A projected coastline/landmass layer (\code{sf} or convertible). When
supplied, net displacements follow the shortest in-water path; otherwise linear (great-circle)
distances are used.}

\item{epsg.code}{Optional. Coordinate reference system used to project positions. If not
supplied, the CRS is taken from \code{land.shape} (when available).}

\item{id.col}{Name of the column containing animal IDs. Defaults to \code{"ID"}.}

\item{timebin.col}{Name of the column containing time bins (in POSIXct format). Defaults to \code{"timebin"}.}

\item{lon.col}{Name of the column containing longitude (or projected x) values. Defaults to \code{"lon"}.}

\item{lat.col}{Name of the column containing latitude (or projected y) values. Defaults to \code{"lat"}.}

\item{dist.col}{Name of the column containing the (stepwise) distance values, in metres.
Defaults to \code{"dist_m"}.}

\item{...}{Additional arguments passed to \code{\link{calculateStepDistances}} (e.g. \code{grid.resolution},
\code{mov.directions}, \code{cores}), used when computing net displacements.}
}
\value{
A data frame with one row per individual containing: the ID column, \code{net_distance_m}
(shortest distance between the first and last positions, in metres), \code{total_distance_m} (total
path length, in metres) and \code{linearity_index} (their ratio, between 0 and 1).
}
\description{
Computes a movement \strong{linearity (directness) index} for each animal, returning a
tidy, fully numeric table (one row per individual). The index is the ratio between the net
displacement (the shortest in-water path between an individual's first and last positions) and
the total path length actually travelled (the sum of consecutive step distances):
\deqn{LI = net\ displacement / total\ distance.}
Values approach 1 for highly directional (near straight-line) movements and approach 0 for
convoluted, back-and-forth or resident movements.

Net displacements are obtained with \code{\link{calculateStepDistances}}, so when a \code{land.shape} is
supplied they follow the shortest in-water route (consistent with the total distance); otherwise
great-circle distances are used. This is the numeric core behind the \code{LI} column of
\code{\link{movementTable}}.
}
\examples{
data(rays)

# build per-time-bin tracks with stepwise distances
coas <- calculateCOAs(rays)
tracks <- calculateStepDistances(coas, verbose = FALSE)

# movement directness per individual (net displacement / total path length)
calculateLinearityIndex(tracks)

}
\seealso{
\code{\link{calculateStepDistances}}, \code{\link{calculateROM}},
\code{\link{movementTable}}
}
