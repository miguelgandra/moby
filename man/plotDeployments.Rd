% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotDeployments.R
\name{plotDeployments}
\alias{plotDeployments}
\title{Plot receiver operating periods (deployment timeline)}
\usage{
plotDeployments(
  deployments,
  row.by = "receiver",
  group.by = NULL,
  events = NULL,
  deploy.col = "deploy",
  recover.col = "recover",
  end = NULL,
  merge.gaps = 1,
  sort.by = c("group", "start", "name"),
  color.pal = NULL,
  single.color = "grey20",
  bar.height = 0.45,
  dividers = TRUE,
  top.band = "\%Y",
  date.interval = "auto",
  date.start = 1,
  date.format = NULL,
  background.color = "grey96",
  events.pch = 8,
  events.color = "grey15",
  events.cex = 1,
  events.label = "event",
  legend = NULL,
  main = "Periods of operation",
  cex = 1,
  file = NULL,
  width = NULL,
  height = NULL,
  res = 300
)
}
\arguments{
\item{deployments}{A receiver-deployment / station log (e.g. from \code{\link{importDeployments}}),
with at least the \code{row.by} and \code{deploy.col} columns (and usually \code{recover.col}).}

\item{row.by}{Name of the column defining the rows (the operating unit). Defaults to \code{"receiver"};
set to \code{"station"} (or any column) to aggregate operating periods to a coarser unit.}

\item{group.by}{Optional column giving a coarser grouping used to colour the rows and their labels
(e.g. \code{"array"}, \code{"region"}). If NULL, all rows share \code{single.color}.}

\item{events}{Optional data frame of point events to overlay, with a column named as \code{row.by} (the
row key) and a date-time column (\code{time}, or the first POSIXct column). Study-neutral: taggings,
servicing, downloads, etc.}

\item{deploy.col, recover.col}{Names of the deployment and recovery date-time columns. A missing
\code{recover} (still-active deployment) is extended to \code{end}. Default "deploy" / "recover".}

\item{end}{Optional study-end date-time used to close still-active deployments. If NULL, the latest
date in the log is used.}

\item{merge.gaps}{Numeric. Consecutive deployments of a unit separated by \code{<=} this many days are
drawn as a single bar; longer gaps appear as breaks. Defaults to 1.}

\item{sort.by}{Row ordering: \code{"group"} (by \code{group.by}, then first deployment; default), \code{"start"}
(first deployment) or \code{"name"} (alphabetical).}

\item{color.pal}{Colours for the groups. If NULL, a colourblind-safe Okabe-Ito palette.}

\item{single.color}{Colour used when \code{group.by} is NULL. Defaults to "grey20".}

\item{bar.height}{Bar thickness as a fraction of the row spacing. Defaults to 0.45.}

\item{dividers}{Logical; draw a subtle separator line between groups (only when \code{group.by} is set
and \code{sort.by = "group"}, so groups are contiguous). Defaults to TRUE.}

\item{top.band}{A \code{\link[base]{strftime}} format for the labelled band above the panel (e.g.
"\%Y" for years). FALSE to omit. Defaults to "\%Y".}

\item{date.interval}{Controls the x-axis date labels. \code{"auto"} (default) chooses calendar-aware
breaks automatically. A positive integer switches to manual mode - a label every n-th
\code{date.format} unit (e.g. \code{date.format = "\%b"}, \code{date.interval = 3} labels every third month) -
useful for very long or very short spans.}

\item{date.start}{Integer phase/offset for the first manual label (used with a numeric
\code{date.interval}). Defaults to 1.}

\item{date.format}{Optional \code{\link[base]{strftime}} format for the x-axis labels. If NULL,
calendar-aware labels are chosen automatically (and "\%b" in manual mode).}

\item{background.color}{Panel background colour. Defaults to "grey96".}

\item{events.pch, events.color, events.cex, events.label}{Marker symbol, colour, size and legend label
for the \code{events} overlay. Defaults \code{pch = 8}, "grey15", 1, "event".}

\item{legend}{Logical; draw a group/event legend in the right margin. If NULL (default), on when
\code{group.by} is set or \code{events} are supplied.}

\item{main}{Plot title. Defaults to "Periods of operation".}

\item{cex}{Global expansion factor for all text. Defaults to 1.}

\item{file}{Optional output file. If \code{NULL} (the default), the figure is drawn on the current
graphics device - the usual interactive behaviour. If a file path is supplied, moby opens a
graphics device chosen from the file extension (\code{.pdf}, \code{.svg}, \code{.png}, \code{.jpg}/\code{.jpeg},
\code{.tif}/\code{.tiff} or \code{.bmp}), draws the figure to it, and closes the device automatically (also if
an error occurs). For multi-page or batch workflows, keep \code{file = NULL} and manage the device
yourself (e.g. \verb{pdf(...); plot...(); dev.off()}).}

\item{width, height}{Output size in inches. Used \emph{only} when \code{file} is supplied. If \code{NULL}
(default), a size is derived from the figure's structure (e.g. the number of individuals or
panels). These defaults are sensible starting points, \emph{not} guarantees: dense or unusual figures
may still need you to set \code{width}/\code{height} explicitly. The two can be set independently.}

\item{res}{Resolution in pixels per inch, for raster formats only (\code{.png}, \code{.jpg}, \code{.tif},
\code{.bmp}); ignored for vector formats (\code{.pdf}, \code{.svg}). Used only when \code{file} is supplied. Defaults
to 300.}
}
\value{
Invisibly, a tidy per-row coverage table: \code{row}, \code{group}, \code{n_deployments}, \code{first}, \code{last},
\code{active_days}, \code{gap_days} and \code{coverage} (active fraction of the first-to-last span).
}
\description{
Draws a timeline of when each receiver (or station/site) was deployed and actively
listening over the study period - one row per unit, with a bar spanning each \verb{[deploy, recover]}
interval and broken bars where a real coverage gap occurs. It is the visual companion to
\code{\link{checkDeployments}} (which reports metadata problems) and shares the visual language of
\code{\link{plotAbacus}} (calendar axis, labelled year band, group colouring). Because a gap in a
detection record can mean either "animal absent" or "receiver not listening", this figure supplies
the sampling-effort context needed to read the detection plots correctly.
}
\details{
Rows are the unique values of \code{row.by} (a receiver, or a coarser unit such as a station or
site); a unit's deployments are merged into operating-period bars, with consecutive deployments
whose gap is \verb{<= merge.gaps} days fused into one bar and longer gaps left as breaks. Rows can be
coloured and legended by a coarser grouping (\code{group.by}, e.g. array or region). An optional \code{events}
overlay draws neutral markers at points in time on the relevant rows (e.g. taggings, servicing
visits, downloads). The layout is device-stable (inch-anchored band, labels and legend), matching
the rest of the plotting family.
}
\examples{
# receiver operating-period timeline from the deployment log
plotDeployments(rays_deployments)
}
\seealso{
\code{\link{checkDeployments}}, \code{\link{importDeployments}}, \code{\link{plotAbacus}}
}
