% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotMaps.R
\name{plotMaps}
\alias{plotMaps}
\title{Plot home-range maps with movement trajectories}
\usage{
plotMaps(
  data,
  uds = NULL,
  animal.tracks = NULL,
  id.groups = NULL,
  id.col = NULL,
  lon.col = NULL,
  lat.col = NULL,
  land.shape = NULL,
  coastline = getOption("moby.coastline", TRUE),
  epsg.code = NULL,
  land.color = "gray50",
  background.layer = NULL,
  background.color = "#F3F7F7",
  background.pal = NULL,
  background.title = NULL,
  discard.missing = TRUE,
  color.pal = NULL,
  ud.contour = 0.95,
  ud.uncertainty = c("band", "outline", "none"),
  tracks.color = "black",
  tracks.lty = 2,
  tracks.lwd = 0.2,
  plot.detections = TRUE,
  pt.color = c("#009E73", "white", "#D55E00"),
  pt.cex = c(1, 0.6, 1),
  ud.legend = TRUE,
  title.color = "black",
  title.pos = "topleft",
  title.inset = c(-0.08, 0),
  scale.km = NULL,
  scale.color = "black",
  scale.pos = "bottomright",
  scale.inset = 0.05,
  extent.factor = 1.1,
  main = NULL,
  cex = 1,
  ncol = NULL,
  file = NULL,
  width = NULL,
  height = NULL,
  res = 300
)
}
\arguments{
\item{data}{A \code{mobyData} or data frame of animal positions (centres of activity) with lon/lat.}

\item{uds}{Utilisation distributions from \code{\link{calculateUDs}} - either KDE (\code{estUD}/\code{estUDm},
drawn as graded volume isopleths) or AKDE (drawn from the stored isopleth contours, with a
confidence envelope; see \code{ud.uncertainty}). Pass the whole \code{calculateUDs()} output; the method is
detected automatically.}

\item{animal.tracks}{The distance-enriched output of \code{\link{calculateStepDistances}} (tracks
are read from its \code{"trajectories"} attribute), or a pre-extracted list of per-individual
linestring geometries.}

\item{id.groups}{Optional named list of ID groups (one block of panels each).}

\item{id.col}{Name of the column containing animal IDs. Defaults to \code{"ID"}.}

\item{lon.col}{Name of the column containing longitude (or projected x) values. Defaults to \code{"lon"}.}

\item{lat.col}{Name of the column containing latitude (or projected y) values. Defaults to \code{"lat"}.}

\item{land.shape}{Optional \code{sf} coastline/landmass polygon drawn over the background.}

\item{coastline}{When no \code{land.shape} is supplied, draw a default coastline for the map extent.
\code{TRUE} auto-picks a scale; a string (\code{"small"}/\code{"medium"}/\code{"large"}) forces an \code{rnaturalearth}
scale; \code{FALSE} draws no coastline. Defaults to \code{getOption("moby.coastline", TRUE)} (set that
option to switch the fallback off package-wide). Requires the (Suggested) \code{rnaturalearth} (with
\code{rnaturalearthdata}) or \code{maps} package; if neither is installed, no coastline is drawn.}

\item{epsg.code}{Projected CRS (numeric EPSG code or \code{crs} object, metre units) for the map. If
NULL, inferred from \code{land.shape}, then from \code{uds}.}

\item{land.color}{Colour of land areas. Defaults to "gray50".}

\item{background.layer}{Optional projected raster displayed behind the home range.}

\item{background.color}{Solid background colour when no \code{background.layer} is supplied.
Defaults to "#F3F7F7".}

\item{background.pal}{Colour palette for the \code{background.layer} raster. If NULL, a muted
bathymetry palette is used.}

\item{background.title}{Legend title for the background layer. Defaults to "Layer values".}

\item{discard.missing}{Logical; drop individuals with fewer than 5 detections (UD needs >= 5).
Defaults to TRUE.}

\item{color.pal}{Colour palette for the home-range isopleths. If NULL, viridis is used.}

\item{ud.contour}{Outer utilisation isopleth to display, as a proportion in (0, 1]. Cells outside
this contour are made transparent. e.g. 0.95 shows the 95\% home range. Defaults to 0.95.}

\item{ud.uncertainty}{For AKDE UDs only: how to draw the confidence envelope of the outer isopleth.
\code{"band"} (default) shades the region between the low and high contours; \code{"outline"} draws the low
and high contours as dashed outlines; \code{"none"} draws only the estimate. Ignored for KDE (which has
no CI).}

\item{tracks.color, tracks.lty, tracks.lwd}{Colour, line type and width of the movement trajectories.}

\item{plot.detections}{Logical; overlay detection points. Defaults to TRUE.}

\item{pt.color}{Colour(s) for detection points: one colour, or three for the first / intermediate
/ last detection. Defaults to c("#009E73", "white", "#D55E00").}

\item{pt.cex}{Size(s) for detection points: one value, or three for first / intermediate / last.
Defaults to c(1, 0.6, 1).}

\item{ud.legend}{Logical; draw the home-range isopleth legend. Defaults to TRUE.}

\item{title.color}{Colour of the per-panel ID label. Defaults to "black".}

\item{title.pos}{Keyword position of the ID label (e.g. "topleft"). Defaults to "topleft".}

\item{title.inset}{Inset of the ID label from the plot edges (single value or x/y vector).
Defaults to c(-0.08, 0).}

\item{scale.km}{Scale-bar length in kilometres. If NULL (default), ~20\% of the map width.}

\item{scale.color}{Colour of the scale-bar labels. Defaults to "black".}

\item{scale.pos}{Keyword position of the scale bar. Defaults to "bottomright".}

\item{scale.inset}{Inset of the scale bar from the plot edges. Defaults to 0.05.}

\item{extent.factor}{Bounding-box expansion factor around the positions. Defaults to 1.1.}

\item{main}{Optional overall title above the panel grid.}

\item{cex}{Global expansion factor for all plot text (ID labels, legends, scale bar). Defaults to 1.}

\item{ncol}{Number of panel columns. If NULL, set from the number of individuals.}

\item{file}{Optional output file. If \code{NULL} (the default), the figure is drawn on the current
graphics device - the usual interactive behaviour. If a file path is supplied, moby opens a
graphics device chosen from the file extension (\code{.pdf}, \code{.svg}, \code{.png}, \code{.jpg}/\code{.jpeg},
\code{.tif}/\code{.tiff} or \code{.bmp}), draws the figure to it, and closes the device automatically (also if
an error occurs). For multi-page or batch workflows, keep \code{file = NULL} and manage the device
yourself (e.g. \verb{pdf(...); plot...(); dev.off()}).}

\item{width, height}{Output size in inches. Used \emph{only} when \code{file} is supplied. If \code{NULL}
(default), a size is derived from the figure's structure (e.g. the number of individuals or
panels). These defaults are sensible starting points, \emph{not} guarantees: dense or unusual figures
may still need you to set \code{width}/\code{height} explicitly. The two can be set independently.}

\item{res}{Resolution in pixels per inch, for raster formats only (\code{.png}, \code{.jpg}, \code{.tif},
\code{.bmp}); ignored for vector formats (\code{.pdf}, \code{.svg}). Used only when \code{file} is supplied. Defaults
to 300.}
}
\value{
Invisibly, a tidy data frame with one row per mapped individual: the isopleth level, the
home-range area (km^2, when coordinates are metric) and the number of detections.
}
\description{
Draws one map per individual showing its kernel utilisation distribution (UD home
range) as graded utilisation isopleths, optionally over a coastline and a background raster
(e.g. bathymetry or temperature), together with inferred movement tracks and time-ordered
detection points. Panels can be grouped by \code{id.groups}.
}
\details{
The home range is drawn as true \strong{utilisation isopleths}: the UD is converted to a
volume distribution with \code{\link[adehabitatHR]{getvolumeUD}}, so \code{ud.contour = 0.95} really
masks everything outside the 95\% home range, and the colour ramp and legend encode the isopleth
level (core = brightest). All layers (coordinates, coastline, background raster) are reconciled to
one projected CRS via the shared spatial pipeline before plotting.
}
\examples{
\dontrun{
pdf("./home-range-maps.pdf", width = 12, height = 20)
plotMaps(data = animal_coas, uds = kud_output, animal.tracks = tracks,
         land.shape = coastline, id.col = "ID", lon.col = "lon", lat.col = "lat")
dev.off()
}
}
\seealso{
\code{\link{calculateUDs}}, \code{\link{calculateStepDistances}}, \code{\link{plotMovements}}
}
