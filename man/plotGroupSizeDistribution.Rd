% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotGroupSizeDistribution.R
\name{plotGroupSizeDistribution}
\alias{plotGroupSizeDistribution}
\title{Plot the distribution of co-occurring group sizes}
\usage{
plotGroupSizeDistribution(
  data,
  id.col = NULL,
  timebin.col = NULL,
  station.col = NULL,
  id.groups = NULL,
  split.by = NULL,
  group.comparisons = "all",
  levels.order = NULL,
  color.pal = NULL,
  background.color = "grey96",
  annotate = TRUE,
  main = NULL,
  legend = TRUE,
  legend.pos = "topright",
  legend.horiz = FALSE,
  ncol = 1,
  cex = 1,
  file = NULL,
  width = NULL,
  height = NULL,
  res = 300
)
}
\arguments{
\item{data}{A \code{mobyData} object or a data frame of binned detections.}

\item{id.col}{Name of the column containing animal IDs. Defaults to \code{"ID"}.}

\item{timebin.col}{Name of the column containing time bins (in POSIXct format). Defaults to \code{"timebin"}.}

\item{station.col}{Name of the column containing station/receiver IDs. Defaults to \code{"station"}.}

\item{id.groups}{Optional named list of ID groups (e.g. species), each drawn in its own panel.}

\item{split.by}{Optional name of a time-bin variable (e.g. diel phase, season) by which to split
each distribution into a stacked bar. If NULL (default), the distribution is computed over the
whole study.}

\item{group.comparisons}{For \code{id.groups}: \code{"within"} (intra-class), \code{"between"} (inter-class pairs)
or \code{"all"} (both). Defaults to \code{"all"}.}

\item{levels.order}{Optional integer vector giving the preferred stacking order of the \code{split.by}
levels.}

\item{color.pal}{Fill colour(s): one per \code{split.by} level (or a single colour when \code{split.by} is
NULL). If NULL, a colourblind-safe palette is used.}

\item{background.color}{Panel background colour. Defaults to "grey96".}

\item{annotate}{Logical; annotate each bar with its total count (\verb{n=}). Defaults to TRUE.}

\item{main}{Optional overall title.}

\item{legend}{Logical; draw the \code{split.by} legend. Defaults to TRUE.}

\item{legend.pos}{Keyword position for the legend. Defaults to "topright".}

\item{legend.horiz}{Logical; draw the legend horizontally. Defaults to FALSE.}

\item{ncol}{Number of columns in the panel layout. Defaults to 1.}

\item{cex}{Global expansion factor for all plot text. Defaults to 1.}

\item{file}{Optional output file. If \code{NULL} (the default), the figure is drawn on the current
graphics device - the usual interactive behaviour. If a file path is supplied, moby opens a
graphics device chosen from the file extension (\code{.pdf}, \code{.svg}, \code{.png}, \code{.jpg}/\code{.jpeg},
\code{.tif}/\code{.tiff} or \code{.bmp}), draws the figure to it, and closes the device automatically (also if
an error occurs). For multi-page or batch workflows, keep \code{file = NULL} and manage the device
yourself (e.g. \verb{pdf(...); plot...(); dev.off()}).}

\item{width, height}{Output size in inches. Used \emph{only} when \code{file} is supplied. If \code{NULL}
(default), a size is derived from the figure's structure (e.g. the number of individuals or
panels). These defaults are sensible starting points, \emph{not} guarantees: dense or unusual figures
may still need you to set \code{width}/\code{height} explicitly. The two can be set independently.}

\item{res}{Resolution in pixels per inch, for raster formats only (\code{.png}, \code{.jpg}, \code{.tif},
\code{.bmp}); ignored for vector formats (\code{.pdf}, \code{.svg}). Used only when \code{file} is supplied. Defaults
to 300.}
}
\value{
Invisibly, a tidy data frame of the plotted distribution (columns \code{series}, \code{group_size},
\code{split_level}, \code{count}, \code{freq}).
}
\description{
Computes and plots the frequency distribution of \strong{co-occurring group sizes} - how
often 2, 3, 4, ... individuals were detected together (at the same station within a time bin) over
the study. It is the aggregation-size view of co-occurrence, complementing the pairwise view
(\code{\link{plotAssociations}}) and the per-location view
(\code{\link{plotStationStats}} with \code{type = "co-occurrences"}). The distribution can be split by a
time-bin variable (e.g. diel phase) into a stacked bar chart, and computed within/between animal
classes via \code{id.groups} and \code{group.comparisons}.
}
\details{
The input is standard detection data (a \code{mobyData} or a data frame); the wide
\verb{time-bin x individual} table needed for co-occurrence is built internally via
\code{\link{createWideTable}}. For each time bin, clusters of individuals sharing a station are
found and their sizes tallied. When \code{id.groups} is set, a \code{"within"} comparison counts clusters of
same-class individuals and a \code{"between"} comparison counts clusters spanning 2+ classes (checked
per cluster). Bar height is the overall frequency of each group size (as a percentage of all
co-occurrence events in that panel); \code{split.by} partitions each bar by a time-bin category.

The computed distribution is returned invisibly as a tidy data frame.
}
\note{
The layout and legend are sized in inch units, so the figure stays consistent across
datasets and graphics devices.
}
\examples{
# Frequency of co-occurring group sizes (one panel per ID group in 'rays')
plotGroupSizeDistribution(rays)
}
