% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotStationStats.R
\name{plotStationStats}
\alias{plotStationStats}
\title{Plot receiver-based statistics}
\usage{
plotStationStats(
  data,
  type = "detections",
  value.scale = c("natural", "count", "proportion"),
  id.col = NULL,
  timebin.col = NULL,
  station.col = NULL,
  aggregate.by = NULL,
  id.groups = NULL,
  group.comparisons = "all",
  color.pal = NULL,
  background.color = "grey96",
  station.labels = c("names", "rotated", "numbered"),
  annotate = c("auto", "count", "proportion", "both", "none"),
  annot.min = NULL,
  main = NULL,
  xlab = NULL,
  legend = TRUE,
  cex = 1,
  file = NULL,
  width = NULL,
  height = NULL,
  res = 300,
  ...
)
}
\arguments{
\item{data}{A data frame containing animal detections with corresponding time-bins.}

\item{type}{One or more statistics to plot (each drawn in its own panel row): \code{"detections"},
\code{"average detections"}, \code{"individuals"}, \code{"co-occurrences"}. Defaults to \code{"detections"}.}

\item{value.scale}{What the bar height encodes: \code{"natural"} (default; counts for count-based statistics,
a proportion for \code{"average detections"}), \code{"count"}, or \code{"proportion"}.}

\item{id.col}{Name of the column containing animal IDs. Defaults to \code{"ID"}.}

\item{timebin.col}{Name of the column containing time bins (in POSIXct format). Defaults to \code{"timebin"}.}

\item{station.col}{Name of the column containing station/receiver IDs. Defaults to \code{"station"}.}

\item{aggregate.by}{Optional column name to summarise by (e.g. habitat or region); defaults to
\code{station.col}. Each station must map to a single \code{aggregate.by} value.}

\item{id.groups}{Optional named list of ID groups, used to split animals into classes (e.g.
species); each becomes a panel column.}

\item{group.comparisons}{For co-occurrences with \code{id.groups}: \code{"within"} (intra-group), \code{"between"}
(inter-group pairs) or \code{"all"} (both). Defaults to \code{"all"}.}

\item{color.pal}{Fill colours, one per \code{type}. If NULL, a colourblind-safe palette is used.}

\item{background.color}{Panel background colour. Defaults to "grey96".}

\item{station.labels}{How to label locations on the x-axis: \code{"names"} (default), \code{"rotated"}
(vertical names) or \code{"numbered"} (numbers, with a name key drawn in the right margin).}

\item{annotate}{Bar annotation: \code{"auto"} (default; the quantity the axis does not show),
\code{"count"}, \code{"proportion"}, \code{"both"} or \code{"none"}.}

\item{annot.min}{Optional numeric; hide annotations for bars below this count (reduces clutter).}

\item{main}{Optional overall title.}

\item{xlab}{Optional x-axis title (defaults to the title-cased \code{aggregate.by} name).}

\item{legend}{Logical; when \code{station.labels = "numbered"}, draw the number-to-name key. Defaults
to TRUE.}

\item{cex}{Global expansion factor for all plot text. Defaults to 1.}

\item{file}{Optional output file. If \code{NULL} (the default), the figure is drawn on the current
graphics device - the usual interactive behaviour. If a file path is supplied, moby opens a
graphics device chosen from the file extension (\code{.pdf}, \code{.svg}, \code{.png}, \code{.jpg}/\code{.jpeg},
\code{.tif}/\code{.tiff} or \code{.bmp}), draws the figure to it, and closes the device automatically (also if
an error occurs). For multi-page or batch workflows, keep \code{file = NULL} and manage the device
yourself (e.g. \verb{pdf(...); plot...(); dev.off()}).}

\item{width, height}{Output size in inches. Used \emph{only} when \code{file} is supplied. If \code{NULL}
(default), a size is derived from the figure's structure (e.g. the number of individuals or
panels). These defaults are sensible starting points, \emph{not} guarantees: dense or unusual figures
may still need you to set \code{width}/\code{height} explicitly. The two can be set independently.}

\item{res}{Resolution in pixels per inch, for raster formats only (\code{.png}, \code{.jpg}, \code{.tif},
\code{.bmp}); ignored for vector formats (\code{.pdf}, \code{.svg}). Used only when \code{file} is supplied. Defaults
to 300.}

\item{...}{Further arguments passed to \code{\link[graphics]{barplot}}.}
}
\value{
Invisibly, a tidy data frame of the plotted statistics (columns \code{series}, \code{type},
\code{location}, \code{count}, \code{proportion}).
}
\description{
Computes and plots summary statistics across receivers (or any coarser spatial unit
via \code{aggregate.by}) from passive acoustic telemetry data, as bar charts. Available statistics are
the number of \code{"detections"}, the mean within-individual detection share (\code{"average detections"}),
the number of unique \code{"individuals"}, and the number of \code{"co-occurrences"} (time-bins where two or
more animals share a location). Data can be split by animal class through \code{id.groups}.
}
\details{
Each requested \code{type} is drawn in its own row of panels with its own axis and unit, so
quantities with different denominators are never conflated on a shared scale. When \code{id.groups} is
supplied, each group (and, for co-occurrences, each between-group pair when \code{group.comparisons} is
\code{"between"}/\code{"all"}) becomes a column, sharing the per-type y-scale so groups are directly
comparable. Bar height encodes counts for count-based statistics and a proportion for
\code{"average detections"} by default (\code{value.scale} overrides this); the complementary quantity is shown as
a bar annotation.

The computed statistics are returned invisibly as a tidy data frame, so the plotted numbers are
available programmatically.
}
\note{
The layout (margins, the optional numbered-location key) is sized in inch units, so it stays
consistent across datasets and graphics devices.
}
\examples{
# number of detections and of unique individuals per receiver
plotStationStats(rays, type = c("detections", "individuals"))
}
