% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculateCOAs.R
\name{calculateCOAs}
\alias{calculateCOAs}
\title{Calculate Centers of Activity (COAs)}
\usage{
calculateCOAs(
  data,
  id.col = NULL,
  timebin.col = NULL,
  lon.col = NULL,
  lat.col = NULL,
  station.col = NULL
)
}
\arguments{
\item{data}{A data frame containing animal detections and including a time bin column
(as specified by the \code{timebin.col} argument). Time bins can be created using the
\code{\link{getTimeBins}} function.}

\item{id.col}{Name of the column containing animal IDs. Defaults to \code{"ID"}.}

\item{timebin.col}{Name of the column containing time bins (in POSIXct format). Defaults to \code{"timebin"}.}

\item{lon.col}{Name of the column containing longitude (or projected x) values. Defaults to \code{"lon"}.}

\item{lat.col}{Name of the column containing latitude (or projected y) values. Defaults to \code{"lat"}.}

\item{station.col}{Name of the column containing station/receiver IDs. Defaults to \code{"station"}.}
}
\value{
A data frame of per-(ID, time bin) positions with columns:
\itemize{
\item The unique identifier (\code{id.col}) for each individual.
\item The time bin (\code{timebin.col}) for which the COA was calculated.
\item Mean longitude and latitude (\code{lon.col} and \code{lat.col}) for each ID and time bin.
\item The number of detections (\code{detections}) for each ID and time bin.
\item The number of unique stations visited (\code{stations}) for each ID and time bin.
\item For numeric columns: Mean values for each ID and time bin.
\item For character or factor columns: Concatenated unique values (separated by "|") for each ID and time bin.
}
When the input is a \code{\link{mobyData}}, the result is also a \code{mobyData}: the input's metadata
(CRS/\code{epsg.code}, tagging dates, \code{id.groups}, land layer) is carried forward - so downstream
spatial analyses such as \code{\link{calculateStepDistances}} and \code{\link{calculateUDs}}
inherit it automatically. (The raw datetime column does not survive aggregation, so \code{datetime.col}
is not retained.)
}
\description{
This function calculates position estimates (Centers of Activity, COAs)
from presence data collected at multiple receivers. It uses weighted means of longitude
and latitude based on detection counts during specified time bins. Additionally, it aggregates
all remaining columns dynamically. For numeric columns, the mean is calculated, while for
character or factor columns, unique values are concatenated and separated by "|".
}
\examples{
data(rays)

# calculate Centers of Activity (one position per ID and time bin)
coas <- calculateCOAs(rays)
head(coas)

}
