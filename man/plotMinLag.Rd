% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotMinLag.R
\name{plotMinLag}
\alias{plotMinLag}
\title{Diagnose the min_lag false-detection threshold}
\usage{
plotMinLag(
  data,
  nominal.delay = NULL,
  factors = c(20, 30, 50),
  id.col = NULL,
  datetime.col = NULL,
  station.col = NULL,
  remove.duplicates = TRUE,
  bar.color = "#0072B2",
  background.color = "grey96",
  breaks = 40,
  cex = 1,
  main = NULL,
  file = NULL,
  width = NULL,
  height = NULL,
  res = 300
)
}
\arguments{
\item{data}{A \code{mobyData} object or a data frame of detections.}

\item{nominal.delay}{Transmitter nominal (mean) delay, in seconds. A single value applied to all
individuals, or a vector named by \code{id.col} for mixed tag families. Read from the \code{mobyData}
metadata (\code{nominal.delay}) when not supplied. Required: the diagnostic normalises \code{min_lag} by it.}

\item{factors}{Reference threshold multipliers (of the nominal delay) to overlay on the plot and
tabulate. Defaults to \code{c(20, 30, 50)}; the middle value is the \code{\link[=filterDetections]{filterDetections()}} default.}

\item{id.col}{Name of the column with animal IDs. Resolved from the metadata / canonical default
("ID") when not supplied.}

\item{datetime.col}{Name of the column with detection timestamps (\code{POSIXct}). Resolved from the
metadata / canonical default ("datetime") when not supplied.}

\item{station.col}{Name of the receiver/station column. Resolved from the metadata / canonical
default ("station") when not supplied.}

\item{remove.duplicates}{Logical; drop exact-duplicate records (same ID, timestamp and station)
before computing \code{min_lag}, matching the stage-0 behaviour of \code{\link[=filterDetections]{filterDetections()}}. Defaults to TRUE.}

\item{bar.color}{Fill colour for the histogram. Defaults to a colourblind-safe blue.}

\item{background.color}{Panel background colour. Defaults to "grey96"; \code{NA} draws none.}

\item{breaks}{Number of histogram bins on the log10 axis. Defaults to 40.}

\item{cex}{Global expansion factor for labels. Defaults to 1.}

\item{main}{Optional plot title.}

\item{file}{Optional output file. If \code{NULL} (the default), the figure is drawn on the current
graphics device - the usual interactive behaviour. If a file path is supplied, moby opens a
graphics device chosen from the file extension (\code{.pdf}, \code{.svg}, \code{.png}, \code{.jpg}/\code{.jpeg},
\code{.tif}/\code{.tiff} or \code{.bmp}), draws the figure to it, and closes the device automatically (also if
an error occurs). For multi-page or batch workflows, keep \code{file = NULL} and manage the device
yourself (e.g. \verb{pdf(...); plot...(); dev.off()}).}

\item{width, height}{Output size in inches. Used \emph{only} when \code{file} is supplied. If \code{NULL}
(default), a size is derived from the figure's structure (e.g. the number of individuals or
panels). These defaults are sensible starting points, \emph{not} guarantees: dense or unusual figures
may still need you to set \code{width}/\code{height} explicitly. The two can be set independently.}

\item{res}{Resolution in pixels per inch, for raster formats only (\code{.png}, \code{.jpg}, \code{.tif},
\code{.bmp}); ignored for vector formats (\code{.pdf}, \code{.svg}). Used only when \code{file} is supplied. Defaults
to 300.}
}
\value{
Invisibly, a data frame with one row per \code{factors} value: the multiplier (\code{factor}), the
threshold in seconds (\code{threshold_s}; \code{NA} when \code{nominal.delay} varies across individuals), and the
number (\code{n_flagged}) and percentage (\code{pct_flagged}) of detections flagged - i.e. with \code{min_lag}
above the threshold, or a lone decode - at that multiplier.
}
\description{
Plots the empirical distribution of \code{min_lag} - each detection's time gap to the nearest other
detection of the same transmitter on the same receiver - normalised by the transmitter nominal
delay, and reports the proportion of detections that the short-interval false-detection filter of
\code{\link[=filterDetections]{filterDetections()}} would flag at several candidate thresholds. It is a quick check on whether the
default \code{min.lag.factor = 30} is appropriate for a given dataset, rather than assuming it.
}
\details{
A genuinely present tag produces bursts of closely spaced detections (small \code{min_lag}), whereas an
isolated spurious decode has no nearby companion (large \code{min_lag}). The two typically appear as
separate modes on the log axis, and a defensible threshold sits in the valley between them. Read the
plot rather than a single number: if the proportion flagged changes little across the reference
multipliers, the choice is robust; if it changes sharply, the threshold matters and should be
justified for that dataset.

\code{min_lag} is computed with the same internal helper used by \code{\link[=filterDetections]{filterDetections()}}, so - on the same
input - the flagged proportions reported here match what the filter would remove. The diagnostic
applies only the optional duplicate-removal step (not the pre-tagging / cut-off steps), so run it on
the detections you intend to filter.
}
\examples{
# assess whether the default 30x min_lag threshold suits the data (120 s nominal delay)
flagged <- plotMinLag(rays, nominal.delay = 120)
flagged

}
\seealso{
\code{\link[=filterDetections]{filterDetections()}}
}
