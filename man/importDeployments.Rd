% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importData.R
\name{importDeployments}
\alias{importDeployments}
\title{Import and harmonise receiver deployment metadata}
\usage{
importDeployments(
  x,
  source = c("vue", "glatos", "otn", "etn", "generic"),
  tz = "UTC",
  col.map = NULL,
  datetime.format = NULL,
  verbose = getOption("moby.verbose", TRUE)
)
}
\arguments{
\item{x}{A path to a \code{.csv}/\code{.xlsx} deployment log, or a data frame (e.g. the output of
\code{etn::get_acoustic_deployments()}).}

\item{source}{One of \code{"vue"}, \code{"glatos"}, \code{"otn"}, \code{"etn"} or \code{"generic"}.}

\item{tz}{Time zone used to parse deploy/recover date-times. Defaults to \code{"UTC"}.}

\item{col.map}{Optional named list mapping canonical deployment fields to source column name(s),
merged over the \code{source} preset. See \link{moby_import_schema} for the full list of deployment fields
and which are required.}

\item{datetime.format}{Optional explicit \code{strptime} format for the deploy/recover columns. When \code{NULL}
(default) the layout is inferred, but only when the answer is unambiguous: a column such as
\code{"07/06/2013"}, where day-first and month-first both fit and disagree, raises an error asking for
this argument rather than silently choosing one. A column no layout can read is an error too, so an
unreadable column can never travel on as silent \code{NA}s.

Timezone handling depends on the input type. TEXT carries no zone, so \code{tz} is applied as the zone
while parsing. A column that is ALREADY \code{POSIXct} (as when a data frame is passed in, e.g. from an
API) is an absolute instant chosen by the caller: it is never reinterpreted, and \code{tz} changes only
how it is displayed.}

\item{verbose}{Logical; print a summary of the operation. Defaults to
\code{getOption("moby.verbose", TRUE)}.}
}
\value{
A data frame with columns \code{receiver}, \code{station}, \code{lon}, \code{lat}, \code{deploy} (POSIXct),
\code{recover} (POSIXct) and, where available, \code{depth}; sorted by receiver and deployment date.
}
\description{
Reads a receiver-deployment / station log from a range of common sources and
harmonises it into a consistent schema for use with \code{\link{checkDeployments}} and the rest
of the \code{moby} workflow.
}
\examples{
# read a raw ETN deployment export and harmonise it
csv <- system.file("extdata", "rays_deployments.csv", package = "moby")
deployments <- importDeployments(csv, source = "etn")
head(deployments)

}
\seealso{
\code{\link{moby_import_schema}} for the canonical field list;
\code{\link{importDetections}}, \code{\link{checkDeployments}}
}
