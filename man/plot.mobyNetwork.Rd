% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mobyNetwork.R
\name{plot.mobyNetwork}
\alias{plot.mobyNetwork}
\title{Plot a moby network}
\usage{
\method{plot}{mobyNetwork}(x, ...)
}
\arguments{
\item{x}{A \code{mobyNetwork} object.}

\item{...}{Further arguments passed to the underlying plotting routine:
\code{\link{plotAssociations}} for association networks (e.g. \code{color.by}, \code{nodes.size},
\code{edge.color}) or \code{\link{plotMovements}} for movement networks (e.g. \code{land.shape},
\code{epsg.code}, \code{background.layer}, \code{edge.metric}, \code{repel.nodes}).}
}
\value{
Called for its side effect (a plot); invisibly returns \code{NULL}.
}
\description{
S3 \code{plot} method for \code{\link{mobyNetwork}} objects. For an \code{"association"}
network it draws the individual co-occurrence/association network (delegating to
\code{\link{plotAssociations}}). For a \code{"movement"} network it draws the directed transition
network between locations over a map (delegating to \code{\link{plotMovements}}), placing nodes
at their geographic coordinates when available.
}
\examples{
data(rays)
# association network (individual co-occurrences)
wide <- createWideTable(rays, value.col = "station")
assoc <- calculateAssociations(wide)
if (requireNamespace("qgraph", quietly = TRUE)) {
  plot(assoc)
}
# movement network (transitions between locations)
trans <- calculateTransitions(rays, spatial.col = "station")
plot(trans)

}
\seealso{
\code{\link{calculateAssociations}}, \code{\link{calculateTransitions}},
\code{\link{plotAssociations}}, \code{\link{plotMovements}}
}
