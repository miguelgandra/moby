% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transitionsTable.R
\name{transitionsTable}
\alias{transitionsTable}
\title{Summarise a movement network as a transitions table}
\usage{
transitionsTable(network, id.metadata = NULL, error.stat = "se")
}
\arguments{
\item{network}{A \code{mobyNetwork} object of type \code{"movement"}, from \code{\link{calculateTransitions}}.}

\item{id.metadata}{Optional data frame of per-animal metadata. Must contain an animal-ID column
matching the network's \code{id.col}. Numeric columns are summarised as mean +/- error and
categorical columns as counts, per transition type.}

\item{error.stat}{Error statistic for numeric metadata summaries: \code{"se"} (standard error,
default) or \code{"sd"} (standard deviation).}
}
\value{
A data frame with one row per directed transition (and group-label rows when the
network was built with \code{id.groups}).
}
\description{
Produces a publication-ready summary table from a movement network (the output of
\code{\link{calculateTransitions}}). Each row is a directed transition between two locations,
with the number of movements, the number (and percentage) of distinct individuals performing
it, and the mean transit duration. When per-animal metadata is supplied, numeric variables are
summarised as mean +/- error and categorical variables as level counts, per transition type.

This is the formatting counterpart to \code{\link{calculateTransitions}} (which holds the
numeric network), mirroring the \code{\link{calculateResidency}} / \code{\link{summaryTable}}
split. It is purely a table: network visualisation is handled by \code{plot()} on the network
object, and temporal distributions of transition timing are available from the network's
\code{transition_records} attribute.
}
\examples{
data(rays)
trans <- calculateTransitions(rays, spatial.col = "station")
# publication-ready summary of the directed transitions
transitionsTable(trans)

}
\seealso{
\code{\link{calculateTransitions}}, \code{\link{summaryTable}}
}
