% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotAssociations.R
\name{plotAssociations}
\alias{plotAssociations}
\title{Network representation of pairwise overlaps.}
\usage{
plotAssociations(
  overlaps = NULL,
  random.results = NULL,
  color.by = c("group", "single"),
  scale.nodes.by = NULL,
  discard.missing = FALSE,
  min.val = NULL,
  cut.val = NULL,
  group.order = NULL,
  plot.stats = TRUE,
  legend.inset = c(-0.02, 0),
  network.layout = "spring",
  network.repulsion = 0.1,
  nodes.color = NULL,
  nodes.size = c(1.2, 2.2),
  nodes.label.scale = FALSE,
  nodes.label.color = "white",
  edge.color = NULL,
  edge.curved = 0.5,
  edge.width = 1,
  edge.label.color = NULL,
  edge.label.font = 1,
  background.color = "grey96",
  overlap.line.color = "red2",
  overlap.line.lwd = 3,
  overlap.line.lty = 1,
  hist.side = c("bottom", "right"),
  standardize.edge.weights = TRUE,
  standardize.freqs = FALSE,
  main = NULL,
  ncol = NULL,
  cex = 1,
  file = NULL,
  width = NULL,
  height = NULL,
  res = 300,
  ...
)
}
\arguments{
\item{overlaps}{A data frame containing the pairwise overlap data. Should be the output from the \code{calculateAssociations} function (a mobyNetwork).}

\item{random.results}{The null-model output of \code{\link{randomizeAssociations}}.}

\item{color.by}{How to colour the nodes: "group" (default) assigns a colour per ID group, or
"single" uses a single colour for all nodes.}

\item{scale.nodes.by}{Numeric vector to scale the nodes. Length should match the number of IDs in overlaps.}

\item{discard.missing}{Logical. If TRUE, excludes individuals without detections and null pairwise comparisons from the network.
Defaults to FALSE.}

\item{min.val}{Minimum value for overlap threshold. Can be a single value (used for all networks)
or a vector with one value per network. Defaults to 0.}

\item{cut.val}{Cutoff value for overlap threshold. Can be a single value (used for all networks)
or a vector with one value per network. Defaults to the 90\% quantile of the overlap values.}

\item{group.order}{A vector specifying the order in which each group comparison type should be plotted.
This parameter can also be used to select and plot only a subset of the available comparisons.
When set to \code{NULL} (the default), all comparisons are plotted in their original order.}

\item{plot.stats}{Logical. If TRUE, additional network metrics are plotted
below each network. Defaults to TRUE.}

\item{legend.inset}{Optional. Specifies how far the legend is inset from the
null model histogram margins. If a single value is given, it is used for both margins;
if two values are given, the first is used for x- distance, the second for y-distance.
Defaults to c(-0.02, 0).}

\item{network.layout}{Layout algorithm for the network (see \code{\link[qgraph]{qgraph}}).
Defaults to "spring".}

\item{network.repulsion}{A scalar controlling the repulsion radius in the spring layout
of the network visualization. This value is passed to the \code{repulsion} argument
of \code{qgraph::qgraph}. Lower values (e.g., 0.05) reduce node separation, while higher
values (e.g., 0.5) increase spacing to avoid clustering. Useful for generating
alternative spatial arrangements. Defaults to 0.1.}

\item{nodes.color}{Color(s) for the nodes.}

\item{nodes.size}{Numeric vector of length 2, indicating the minimum and maximum
node sizes. Defaults to c(1, 2.2).}

\item{nodes.label.scale}{Logical. If TRUE, scales node labels. Defaults to FALSE.}

\item{nodes.label.color}{Color of the node labels. Defaults to white.}

\item{edge.color}{Color(s) for the edges. Can be a single value (used for all networks)
or a vector with one value per network. Defaults to NULL, which averages node colors.}

\item{edge.curved}{Numeric value or logical indicating the curvature of the edges. Defaults to 0.5.}

\item{edge.width}{Numeric value scaling all edge widths uniformly (default = 1).
Larger values make all edges thicker, smaller values make them thinner.}

\item{edge.label.color}{Colour of the edge labels. If NULL (default), inherits \code{edge.color}.}

\item{edge.label.font}{Font type for edge labels (1 is plain, 2 is bold, 3 is italic, 4 is bold and italic, 5 is symbol font). Defaults to 1.}

\item{background.color}{Background color of the plot. Defaults to "grey96".}

\item{overlap.line.color}{Color of the line representing observed overlap in the null model plot. Defaults to "red2".}

\item{overlap.line.lwd}{Line width of the observed overlap line. Defaults to 3.}

\item{overlap.line.lty}{Line type of the observed overlap line. Defaults to 1.}

\item{hist.side}{Position of the null model histogram relative to the network plot, either "bottom" or "right". Defaults to "bottom".}

\item{standardize.edge.weights}{Logical. If TRUE, edge widths are standardized across all networks so they are directly comparable. Defaults to TRUE.}

\item{standardize.freqs}{Logical. If TRUE, standardizes frequencies in the null model plot. Defaults to FALSE.}

\item{main}{Optional overall title drawn above the panel grid.}

\item{ncol}{Number of columns for the plot layout. Defaults to NULL.}

\item{cex}{Global expansion factor scaling all text (titles, node/edge labels, axes, legend). Defaults to 1.}

\item{file}{Optional output file. If \code{NULL} (the default), the figure is drawn on the current
graphics device - the usual interactive behaviour. If a file path is supplied, moby opens a
graphics device chosen from the file extension (\code{.pdf}, \code{.svg}, \code{.png}, \code{.jpg}/\code{.jpeg},
\code{.tif}/\code{.tiff} or \code{.bmp}), draws the figure to it, and closes the device automatically (also if
an error occurs). For multi-page or batch workflows, keep \code{file = NULL} and manage the device
yourself (e.g. \verb{pdf(...); plot...(); dev.off()}).}

\item{width, height}{Output size in inches. Used \emph{only} when \code{file} is supplied. If \code{NULL}
(default), a size is derived from the figure's structure (e.g. the number of individuals or
panels). These defaults are sensible starting points, \emph{not} guarantees: dense or unusual figures
may still need you to set \code{width}/\code{height} explicitly. The two can be set independently.}

\item{res}{Resolution in pixels per inch, for raster formats only (\code{.png}, \code{.jpg}, \code{.tif},
\code{.bmp}); ignored for vector formats (\code{.pdf}, \code{.svg}). Used only when \code{file} is supplied. Defaults
to 300.}

\item{...}{Further arguments passed to \code{\link[qgraph]{qgraph}}.}
}
\value{
Invisibly, a tidy per-comparison-type data frame of network statistics (number of dyads,
mean overlap, mean shared monitoring days, mean binary degree, and the edge display thresholds).
}
\description{
This function plots a network of estimated pairwise overlaps between
individuals or a histogram showing the distribution of overlaps from null model permutations,
or both. If only \code{overlaps} are provided, it plots a network where each node represents
an individual, with edges indicating the extent of spatial overlap between pairs.
Thicker edges represent higher overlap. If only \code{random.results} are provided, it plots
a histogram showing the distribution of overlaps from the null model permutations.
This helps to identify whether the observed overlaps are significantly different from
what would be expected by chance. If both are provided, it plots both the network
and the histogram.
}
\examples{
# Pairwise co-occurrence network from a wide time-bin x individual table
wide  <- createWideTable(rays, value.col = "station")
assoc <- calculateAssociations(wide)
if (requireNamespace("qgraph", quietly = TRUE)) {
  plotAssociations(assoc)
}

# Overlay the null-model histogram from a permutation test
\donttest{
if (requireNamespace("qgraph", quietly = TRUE)) {
  rand <- randomizeAssociations(wide, assoc, iterations = 100, random.seed = 1)
  plotAssociations(assoc, rand)
}
}
}
\seealso{
\code{\link{calculateAssociations}}, \code{\link{randomizeAssociations}}, \code{\link[qgraph]{qgraph}}
}
