% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/randomizeTransitions.R
\name{randomizeTransitions}
\alias{randomizeTransitions}
\title{Test movement-network transitions against a null model}
\usage{
randomizeTransitions(
  network,
  iterations = 1000,
  alternative = c("two.sided", "greater", "less"),
  p.adjust.method = "fdr",
  conf.level = 0.95,
  random.seed = NULL,
  verbose = getOption("moby.verbose", TRUE)
)
}
\arguments{
\item{network}{A \code{mobyNetwork} object of type \code{"movement"}, from \code{\link{calculateTransitions}}.}

\item{iterations}{Number of permutations. Defaults to 1000.}

\item{alternative}{Alternative hypothesis: \code{"two.sided"} (default), \code{"greater"} or \code{"less"}.}

\item{p.adjust.method}{Multiple-comparison correction across edges, passed to
\code{\link[stats]{p.adjust}}. Defaults to \code{"fdr"}; use \code{"none"} for raw p-values.}

\item{conf.level}{Confidence level used for the significance label. Defaults to 0.95.}

\item{random.seed}{Optional integer for reproducibility.}

\item{verbose}{Logical; print progress (a permutation progress bar). Defaults to
\code{getOption("moby.verbose", TRUE)}.}
}
\value{
A list with
\item{edges}{A data frame of per-transition results: \code{group}, \code{from}, \code{to}, \code{n_movements}
(observed), \code{mean_null}, \code{p_value}, \code{p_adjusted}, and \code{association} (\code{"more"}, \code{"less"} or
\code{"non-significant"}, based on the adjusted p-value).}
\item{network}{A data frame with a network-level test per group: the observed number of
realised transitions versus the null, with its empirical p-value.}
}
\description{
Tests whether the directed transitions in a movement network (from
\code{\link{calculateTransitions}}) occur more (or less) often than expected under random
movement, using a within-individual permutation null model. For each individual, the
time-ordered sequence of visited locations is randomly permuted and the transition network
recomputed; repeating this many times builds a null distribution for each edge. Empirical,
multiple-comparison-corrected p-values then identify transitions (and the overall network)
that are stronger or weaker than expected.

The null permutes the order in which each individual visited its set of locations, preserving
that set (and hence each animal's site fidelity) while randomising connectivity. It therefore
tests the non-randomness of \emph{movement order / connectivity}, not of site use itself.
Note that movement-network null models are an active area of research; this is one defensible,
transparent choice rather than a universally agreed standard.
}
\examples{
\donttest{
data(rays)
trans <- calculateTransitions(rays, spatial.col = "station")
# test each transition against a within-individual permutation null
# (iterations kept low here for speed; use the default 1000 in practice)
rand <- randomizeTransitions(trans, iterations = 100, random.seed = 1)
head(rand$edges)
rand$network
}

}
\seealso{
\code{\link{calculateTransitions}}, \code{\link{randomizeAssociations}}
}
