% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mobyTable.R
\name{format.mobyTable}
\alias{format.mobyTable}
\title{Format a moby summary table for display or export}
\usage{
\method{format}{mobyTable}(
  x,
  style = c("internal", "report", "concise"),
  symbols = c("ascii", "unicode"),
  decimals = NULL,
  group.by = NULL,
  include.summary.row = TRUE,
  datetime.format = "\%d/\%m/\%Y",
  ...
)
}
\arguments{
\item{x}{A \code{mobyTable} (from \code{\link{summaryTable}}, \code{\link{movementTable}} or
\code{\link{transitionsTable}}).}

\item{style}{Column-name style. \code{"internal"} (default) keeps the snake_case names the API
guarantees; \code{"report"} uses publication-ready headers (\code{n_detections} -> "N Detect");
\code{"concise"} uses the same headers abbreviated for narrow tables. Only the names differ.}

\item{symbols}{Whether the rendered table may use typographic symbols: \code{"ascii"} (default) writes
\verb{+/-}, \code{"unicode"} the plus-minus sign. ASCII is the default because this table is usually
written to a file, and a spreadsheet opening a UTF-8 CSV with no byte-order mark guesses the
encoding. \code{print()} picks the right one for the terminal on its own.}

\item{decimals}{Optional per-column override of the display precision, as a named numeric vector
of decimal places - \code{c(distance_km = 2)}. Merged OVER the built-in precision, so naming one
column leaves the rest untouched, and both the values and the \verb{mean +/- error} row follow it.
Named by the INTERNAL column names, which do not change with \code{style}.}

\item{group.by}{Column to group the rendering by. Defaults to the table's own \code{group} column when
it has one (the \code{id.groups} split), or to no grouping. Each group gets its own
\verb{mean +/- error} row. Pass \code{FALSE} to render ungrouped.}

\item{include.summary.row}{Logical; append the display-only \verb{mean +/- error} row(s). Default
\code{TRUE}. A group of ONE row never gets one: the "mean" would just restate that row.}

\item{datetime.format}{\code{strftime} format for date/time columns. Default \code{"\%d/\%m/\%Y"}.}

\item{...}{Unused.}
}
\value{
A character \code{data.frame}.
}
\description{
Renders a \code{mobyTable} as a character data frame: fixed per-metric precision, missing
values as \code{"-"}, and (where a group has more than one row) a display-only \verb{mean +/- error} row.
This is the export route - \code{write.csv(format(x), "table.csv", row.names = FALSE)} - and the same
rendering \code{print()} shows.

The returned table stays rectangular. Group headings and the blank lines between groups belong to
the console rendering, not to the exported object, so a CSV never carries an empty record.
}
\examples{
data(rays)
tbl <- summaryTable(rays, last.monitoring.date = as.POSIXct("2023-12-31", tz = "UTC"))
# the object itself is typed - you can compute on it
mean(tbl$n_detections)
# ...and format() renders the version you export
head(format(tbl, style = "report"))
}
\seealso{
\code{\link{summaryTable}}, \code{\link{movementTable}}, \code{\link{transitionsTable}}
}
