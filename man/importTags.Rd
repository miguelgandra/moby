% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importData.R
\name{importTags}
\alias{importTags}
\title{Import and harmonise tag / animal metadata}
\usage{
importTags(
  x,
  source = c("vue", "glatos", "otn", "etn", "generic"),
  tz = "UTC",
  col.map = NULL,
  datetime.format = NULL,
  keep.extra = TRUE,
  verbose = getOption("moby.verbose", TRUE)
)
}
\arguments{
\item{x}{A path to a \code{.csv}/\code{.xlsx} tag-metadata file, or a data frame (e.g. from
\code{etn::get_tags()} / \code{etn::get_animals()}).}

\item{source}{One of \code{"vue"}, \code{"glatos"}, \code{"otn"}, \code{"etn"} or \code{"generic"}.}

\item{tz}{Time zone used to parse the tagging date. Defaults to \code{"UTC"}.}

\item{col.map}{Optional named list mapping canonical tag fields to the column name(s) in \code{x},
merged over (and extending) the \code{source} preset. See \link{moby_import_schema} for the full list of tag
fields and examples.}

\item{datetime.format}{Optional explicit \code{strptime} format for the tagging-date column. When \code{NULL}
(default) the layout is inferred, but only when the answer is unambiguous: a column such as
\code{"07/06/2013"}, where day-first and month-first both fit and disagree, raises an error asking for
this argument rather than silently choosing one. A column no layout can read is an error too, so an
unreadable column can never travel on as silent \code{NA}s.

Timezone handling depends on the input type. TEXT carries no zone, so \code{tz} is applied as the zone
while parsing. A column that is ALREADY \code{POSIXct} (as when a data frame is passed in, e.g. from an
API) is an absolute instant chosen by the caller: it is never reinterpreted, and \code{tz} changes only
how it is displayed.}

\item{keep.extra}{Logical; retain unmapped source columns. Defaults to \code{TRUE} so that
additional biometric fields are preserved.}

\item{verbose}{Logical; print a summary of the operation. Defaults to
\code{getOption("moby.verbose", TRUE)}.}
}
\value{
A data frame with at least \code{transmitter} and (when available) \code{ID}, \code{tagging_date}
(POSIXct), \code{nominal_delay} (seconds) and biometric columns.
}
\description{
Reads a tag/animal metadata table and harmonises it into a consistent schema
(\code{transmitter}, \code{ID}, \code{tagging_date}, plus biometrics such as \code{species}, \code{sex}, \code{length}, and the
transmitter \code{nominal_delay} when the source provides one). Feed the result to
\code{\link{matchTags}} to assign animal IDs to a detection dataset, and to
\code{\link{as_moby}}'s \code{tags} argument to attach the per-animal tagging dates and nominal delays.
}
\details{
When the source specifies a delay RANGE (\code{min_delay}/\code{max_delay}) rather than a nominal
delay - as many tag-specification exports do - \code{nominal_delay} is derived as their midpoint. The
nominal delay is what \code{\link{filterDetections}} uses to scale its short-interval (min_lag)
false-detection filter, so carrying it through here means that filter can be enabled automatically.
}
\examples{
# harmonise a tag-metadata table (here the bundled 'rays_tags' data frame)
tags <- importTags(rays_tags, source = "generic",
                   col.map = list(ID = "ID", transmitter = "transmitter",
                                  tagging_date = "tagging_date", species = "species"))
head(tags)

}
\seealso{
\code{\link{moby_import_schema}} for the canonical field list;
\code{\link{matchTags}}, \code{\link{importDetections}}
}
