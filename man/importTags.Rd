% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importData.R
\name{importTags}
\alias{importTags}
\title{Import and harmonise tag / animal metadata}
\usage{
importTags(
  x,
  source = c("vue", "glatos", "otn", "etn", "generic"),
  tz = "UTC",
  col.map = NULL,
  datetime.format = NULL,
  keep.extra = TRUE
)
}
\arguments{
\item{x}{A path to a \code{.csv}/\code{.xlsx} tag-metadata file, or a data frame (e.g. from
\code{etn::get_tags()} / \code{etn::get_animals()}).}

\item{source}{One of \code{"vue"}, \code{"glatos"}, \code{"otn"}, \code{"etn"} or \code{"generic"}.}

\item{tz}{Time zone used to parse the tagging date. Defaults to \code{"UTC"}.}

\item{col.map}{Optional named list overriding/extending the \code{source} preset (e.g.
\code{list(transmitter = "Tag", tagging_date = "Deployed")}).}

\item{datetime.format}{Optional explicit \code{strptime} format for the tagging-date column.}

\item{keep.extra}{Logical; retain unmapped source columns. Defaults to \code{TRUE} so that
additional biometric fields are preserved.}
}
\value{
A data frame with at least \code{transmitter} and (when available) \code{ID}, \code{tagging_date}
(POSIXct), \code{nominal_delay} (seconds) and biometric columns.
}
\description{
Reads a tag/animal metadata table and harmonises it into a consistent schema
(\code{transmitter}, \code{ID}, \code{tagging_date}, plus biometrics such as \code{species}, \code{sex}, \code{length}, and the
transmitter \code{nominal_delay} when the source provides one). Use together with
\code{\link{assignAnimalIDs}} to attach animal IDs, tagging dates and nominal delays to a
detection dataset.
}
\details{
When the source specifies a delay RANGE (\code{min_delay}/\code{max_delay}) rather than a nominal
delay - as many tag-specification exports do - \code{nominal_delay} is derived as their midpoint. The
nominal delay is what \code{\link{filterDetections}} uses to scale its short-interval (min_lag)
false-detection filter, so carrying it through here means that filter can be enabled automatically.
}
\examples{
# harmonise a tag-metadata table (here the bundled 'rays_tags' data frame)
tags <- importTags(rays_tags, source = "generic",
                   col.map = list(ID = "ID", transmitter = "transmitter",
                                  tagging_date = "tagging_date", species = "species"))
head(tags)

}
\seealso{
\code{\link{assignAnimalIDs}}, \code{\link{importDetections}}
}
